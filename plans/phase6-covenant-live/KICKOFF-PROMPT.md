# Phase 6 long-run — the paste-me kickoff (short by design)

> This is the ~1.5k-char prompt you paste to start/restart the run. The full contract is committed
> on int/phase6 and the session reads it — keep this short; put detail in the goal file, not here.

---

Run the Atrium Phase 6 long-run autonomous goal. Tracker: GitHub lmvdz/atrium, map #162. Base branch: **int/phase6** (verify by sha before touching anything — the prototype tree lives only there, not on main).

Read the full contract first — it is committed and current on int/phase6:
- `plans/phase6-covenant-live/LONGRUN-GOAL-PROMPT.md` — the goal, the leading-indicator stop conditions, the long-run discipline, the reserved-for-Lars lines.
- `plans/phase6-covenant-live/DRIVABLE-ACCEPTANCE-RUBRIC.md` — the fixed pass/fail bar.

Goal in one line: drive the covenant-live product to its full destination — two real browsers converging on content AND covenant state, agent-peers, worktree co-replication, the acceptance scenario (#200) green twice at concurrency — **staged and green on int/phase6, ready for Lars's one-command land**. Reaching ready-to-land IS done. Do NOT land to main, do NOT change what the covenant means, do NOT perform the certify/spend ledger acts — those are Lars's.

Autonomy: resolve every decision on the map's recorded authority (the three papers, settled inputs, prior decisions, the ratified digest model), never invented preference; each resolution says so and stays reopenable. Drive the product against the rubric every increment — green gauntlets are not a driven product. Halt-and-surface the instant a leading-indicator stop condition in the goal file trips, or a reserved-for-Lars line is reached. Communicate decisions and results, never waits. Run inside `/campaign`'s loop with the goal file's long-run amendments.
