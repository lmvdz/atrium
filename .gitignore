# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
out/
*.tsbuildinfo

# next.js
.next/
.turbo/
next-env.d.ts

# test / tooling artifacts
coverage/
playwright-report/
test-results/
blob-report/
.playwright/
playwright/.cache/

# env — .env.example is committed, real env files are not
.env
.env.*
!.env.example

# docker volumes / local infra state
.docker-data/

# editor + os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# harness-managed agent worktrees and session state
.claude/

# blind-review scratch (prompt, runner scripts, transcripts) — evidence lives on the ticket
critic-prompt.txt
run-codex.sh
run-grok.sh
codex-r5*.txt
grok-r5*.txt
receipt.md

# the mutant runner's crash-recovery record. Written between applying a mutation
# and undoing it, deleted after; a leftover one is recovered on the next start.
# Never a committed artifact — it holds a file's pre-mutation bytes.
mutants/.inflight.json

# Round 8's scratch lab for #22 — private-Postgres exploit reproductions and
# helper scripts. Evidence, not source; the tests carry what has to survive.
tmp-r8/
