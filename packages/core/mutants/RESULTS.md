# Mutant ledger results — `@atrium/core`

Generated by `node packages/core/mutants/run.mjs`. Do not edit by hand: rerun it.

**144 of 144 caught.**

Each row applies one verbatim substitution to one source file, runs the whole
`@atrium/core` suite, and restores the file. A mutant is *caught* only when the
tests the ledger names for it actually fail — a mutation that trips something
else is recorded as an escape, because the claim being made is that a specific
test pins a specific rule.

**A suite that fails to load is not a catch.** It is recorded as `unpinned` and
fails `--check`: no test ran, so no test named the rule, and a row whose whole
evidence is that nothing happened is the vacuity this ledger exists to rule out.
Every failing test name is listed below its mutant, so a reader can audit the
claim from this file without rerunning anything.

| mutant | file | verdict | detail |
| --- | --- | --- | --- |
| `boundary_parse_dropped_append` | `src/reduce.ts` | caught | 2 failing |
| `boundary_parse_dropped_fold` | `src/reduce.ts` | caught | 4 failing |
| `empty_window_reducer` | `src/authority.ts` | caught | 2 failing |
| `empty_window_engine` | `src/acceptance.ts` | caught | 3 failing |
| `reducer_quote_gate_dropped` | `src/authority.ts` | caught | 3 failing |
| `validator_quote_requirement_dropped` | `src/escalation.ts` | caught | 5 failing |
| `attribution_fallback_restored` | `src/escalation.ts` | caught | 5 failing |
| `quote_bearing_disabled` | `src/escalation.ts` | caught | 41 failing |
| `min_quote_length_unenforced` | `src/escalation.ts` | caught | 2 failing |
| `ambiguous_quote_takes_the_first` | `src/escalation.ts` | caught | 2 failing |
| `bearing_message_takes_the_first` | `src/escalation.ts` | caught | 2 failing |
| `receipt_policy_min_quote_lowered` | `src/policy.ts` | caught | 6 failing |
| `dedup_ignores_polarity` | `src/acceptance.ts` | caught | 7 failing |
| `claim_theta_auto_lowered` | `src/policy.ts` | caught | 12 failing |
| `claim_theta_min_lowered` | `src/policy.ts` | caught | 8 failing |
| `floor_decoupled_from_theta` | `src/policy.ts` | caught | 6 failing |
| `type_certification_removed` | `src/acceptance.ts` | caught | 19 failing |
| `claim_certification_always_true` | `src/policy.ts` | caught | 26 failing |
| `declarative_open_question_allowed` | `src/escalation.ts` | caught | 3 failing |
| `payload_actor_guard_disabled` | `src/events.ts` | caught | 4 failing |
| `receipt_skipped_entirely` | `src/reduce.ts` | caught | 20 failing |
| `payload_binding_disabled` | `src/authority.ts` | caught | 4 failing |
| `provenance_binding_disabled` | `src/authority.ts` | caught | 4 failing |
| `proposer_binding_disabled` | `src/authority.ts` | caught | 48 failing |
| `confidence_floor_disabled` | `src/reduce.ts` | caught | 6 failing |
| `decision_never_auto_disabled` | `src/reduce.ts` | caught | 53 failing |
| `claim_verification_gate_disabled` | `src/reduce.ts` | caught | 17 failing |
| `direct_acceptance_gate_disabled` | `src/reduce.ts` | caught | 14 failing |
| `answers_human_gate_disabled` | `src/reduce.ts` | caught | 6 failing |
| `supersession_policy_narrowed` | `src/reduce.ts` | caught | 8 failing |
| `corrections_human_gate_disabled` | `src/reduce.ts` | caught | 27 failing |
| `blockquote_stripping_neutered` | `src/escalation.ts` | caught | 12 failing |
| `answer_binding_order_check_dropped` | `src/acceptance.ts` | caught | 1 failing |
| `answer_binding_question_open_check_dropped` | `src/acceptance.ts` | caught | 1 failing |
| `attention_reads_its_own_theta` | `src/attention.ts` | caught | 3 failing |
| `attention_priority_tiers_swapped` | `src/attention.ts` | caught | 4 failing |
| `ordering_gate_loosened` | `src/reduce.ts` | caught | 3 failing |
| `consumed_ids_not_spent_on_failure` | `src/reduce.ts` | caught | 35 failing |
| `amend_may_touch_attribution` | `src/reduce.ts` | caught | 1 failing |
| `noop_amend_silent_again` | `src/reduce.ts` | caught | 3 failing |
| `one_concession_marker_deleted` | `src/escalation.ts` | caught | 1 failing |
| `one_future_pattern_deleted` | `src/escalation.ts` | caught | 3 failing |
| `escalation_scan_cap_loosened` | `src/escalation.ts` | caught | 1 failing |
| `rationale_accepts_a_bare_string` | `src/attention.ts` | caught | 2 failing |
| `subject_kind_default_restored` | `src/attention.ts` | caught | 1 failing |
| `counterexample_voice_reverted` | `src/corrections.ts` | caught | 1 failing |
| `blank_window_reducer` | `src/authority.ts` | caught | 1 failing |
| `blank_window_engine` | `src/acceptance.ts` | caught | 1 failing |
| `reducer_quote_gate_rescoped` | `src/authority.ts` | caught | 1 failing |
| `validator_quote_requirement_rescoped` | `src/escalation.ts` | caught | 3 failing |
| `proposal_quote_rescoped` | `src/proposal.ts` | caught | 2 failing |
| `bearing_forgives_interposed_words` | `src/matching.ts` | caught | 89 failing |
| `bearing_forgives_missing_statement_words` | `src/matching.ts` | caught | 114 failing |
| `bearing_forgives_the_tail` | `src/matching.ts` | caught | 43 failing |
| `bearing_forgives_the_statement_tail` | `src/matching.ts` | caught | 1 failing |
| `ordered_tokens_dedup` | `src/matching.ts` | caught | 19 failing |
| `bearing_drops_short_tokens` | `src/matching.ts` | caught | 54 failing |
| `receipt_policy_droppable_restored` | `src/matching.ts` | caught | 5 failing |
| `receipt_policy_align_cap_raised` | `src/policy.ts` | caught | 4 failing |
| `statement_uncheckable_skipped` | `src/escalation.ts` | caught | 1 failing |
| `refer_severity_ignored_reducer` | `src/authority.ts` | caught | 7 failing |
| `refer_severity_ignored_engine` | `src/acceptance.ts` | caught | 20 failing |
| `refer_collapsed_into_reject` | `src/escalation.ts` | caught | 12 failing |
| `attention_certifiable_silence_dropped` | `src/attention.ts` | caught | 1 failing |
| `blank_is_trim` | `src/matching.ts` | caught | 7 failing |
| `normalize_keeps_invisibles` | `src/matching.ts` | caught | 9 failing |
| `ordered_tokens_ascii_only` | `src/matching.ts` | caught | 51 failing |
| `ordered_tokens_drops_punctuation` | `src/matching.ts` | caught | 48 failing |
| `normalize_folds_emphasis` | `src/matching.ts` | caught | 3 failing |
| `quote_anchoring_disabled` | `src/escalation.ts` | caught | 6 failing |
| `quote_anchoring_allows_a_prefix` | `src/matching.ts` | caught | 1 failing |
| `sentence_split_ignores_terminators` | `src/matching.ts` | caught | 6 failing |
| `typographic_apostrophe_unfolded` | `src/matching.ts` | caught | 3 failing |
| `quote_context_unchecked` | `src/escalation.ts` | caught | 11 failing |
| `later_correction_unseen` | `src/escalation.ts` | caught | 14 failing |
| `speech_act_unchecked` | `src/escalation.ts` | caught | 6 failing |
| `refer_below_theta_again` | `src/acceptance.ts` | caught | 1 failing |
| `commitment_model_mintable` | `src/authority.ts` | caught | 20 failing |
| `objective_model_mintable` | `src/authority.ts` | caught | 18 failing |
| `normalize_deletes_backticks` | `src/matching.ts` | caught | 11 failing |
| `normalize_drops_link_destination` | `src/matching.ts` | caught | 4 failing |
| `normalize_refolds_nfkc` | `src/matching.ts` | caught | 6 failing |
| `content_excludes_pictographs` | `src/matching.ts` | caught | 3 failing |
| `receipt_after_confidence_floor` | `src/reduce.ts` | caught | 1 failing |
| `later_correction_cap_fails_open` | `src/escalation.ts` | caught | 1 failing |
| `question_mark_folded_first` | `src/matching.ts` | caught | 2 failing |
| `code_span_whitespace_collapsed` | `src/matching.ts` | caught | 5 failing |
| `dedup_destroys_a_referral` | `src/acceptance.ts` | caught | 1 failing |
| `bidi_controls_deleted` | `src/matching.ts` | caught | 5 failing |
| `later_scan_starts_after_last_citation` | `src/escalation.ts` | caught | 6 failing |
| `question_mark_set_narrowed` | `src/matching.ts` | caught | 1 failing |
| `coverage_reads_the_stripped_text` | `src/escalation.ts` | caught | 1 failing |
| `later_revision_addition_only` | `src/escalation.ts` | caught | 2 failing |
| `later_scan_skips_cited_messages` | `src/escalation.ts` | caught | 2 failing |
| `whitespace_controls_deleted` | `src/matching.ts` | caught | 2 failing |
| `whitespace_run_uses_js_s` | `src/matching.ts` | caught | 1 failing |
| `ordered_tokens_drops_spaces` | `src/matching.ts` | caught | 20 failing |
| `ordered_tokens_drops_apostrophes` | `src/matching.ts` | caught | 6 failing |
| `routing_tokens_keep_spaces` | `src/matching.ts` | caught | 2 failing |
| `align_borne_read_off_the_diff` | `src/matching.ts` | caught | 7 failing |
| `respacing_not_reported` | `src/escalation.ts` | caught | 3 failing |
| `uncheckable_too_long_rejected` | `src/escalation.ts` | caught | 1 failing |
| `later_revision_window_gate_removed` | `src/escalation.ts` | caught | 8 failing |
| `later_revision_gate_reads_the_earliest_citation` | `src/escalation.ts` | caught | 4 failing |
| `token_rendered_raw` | `src/escalation.ts` | caught | 1 failing |
| `bearing_result_ignored` | `src/escalation.ts` | caught | 37 failing |
| `line_break_is_newline_only` | `src/matching.ts` | caught | 1 failing |
| `fragment_scan_cap_reports_a_fragment` | `src/escalation.ts` | caught | 2 failing |
| `link_title_discarded` | `src/matching.ts` | caught | 2 failing |
| `image_marker_discarded` | `src/matching.ts` | caught | 1 failing |
| `later_revision_no_citation_reads_clean` | `src/escalation.ts` | caught | 2 failing |
| `later_revision_blank_statement_reads_clean` | `src/escalation.ts` | caught | 2 failing |
| `reducer_type_certification_removed` | `src/reduce.ts` | caught | 8 failing |
| `commitment_shapes_emptied` | `src/policy.ts` | caught | 15 failing |
| `certification_reads_raw_statement_engine` | `src/acceptance.ts` | caught | 3 failing |
| `certification_reads_raw_statement_reducer` | `src/reduce.ts` | caught | 1 failing |
| `scan_floor_from_citations` | `src/escalation.ts` | caught | 2 failing |
| `adjacent_correction_unscanned` | `src/escalation.ts` | caught | 1 failing |
| `sed_marker_reads_raw_body` | `src/escalation.ts` | caught | 1 failing |
| `cursor_compares_the_spelling` | `src/reduce.ts` | caught | 5 failing |
| `instant_key_truncates_to_milliseconds` | `src/common.ts` | caught | 1 failing |
| `retype_skips_structural_invariants` | `src/reduce.ts` | caught | 2 failing |
| `retype_drops_the_person` | `src/corrections.ts` | caught | 3 failing |
| `answers_edge_ignores_retraction` | `src/reduce.ts` | caught | 1 failing |
| `answers_edge_ignores_already_answered` | `src/reduce.ts` | caught | 1 failing |
| `supersession_is_irreversible` | `src/reduce.ts` | caught | 1 failing |
| `panel_skips_uncertified_readings` | `src/attention.ts` | caught | 2 failing |
| `panel_continues_before_the_invariant` | `src/attention.ts` | caught | 7 failing |
| `panel_drops_self_stated_commitment` | `src/attention.ts` | caught | 2 failing |
| `panel_tells_you_a_colleague_committed_you` | `src/attention.ts` | caught | 2 failing |
| `panel_silences_an_empty_audience` | `src/attention.ts` | caught | 4 failing |
| `reconcile_drops_dismissals` | `src/attention.ts` | caught | 3 failing |
| `fallback_priority_returns_nan` | `src/attention.ts` | caught | 1 failing |
| `mention_items_undeduped` | `src/attention.ts` | caught | 1 failing |
| `item_id_unescaped` | `src/attention.ts` | caught | 1 failing |
| `dedup_folds_compatibility_forms` | `src/acceptance.ts` | caught | 12 failing |
| `dedup_folds_case` | `src/acceptance.ts` | caught | 2 failing |
| `question_ornaments_unknown` | `src/matching.ts` | caught | 1 failing |
| `question_certification_is_the_negation` | `src/escalation.ts` | caught | 1 failing |
| `payload_text_reads_the_wrong_key` | `src/objects.ts` | caught | 7 failing |
| `correction_rate_counts_every_correction` | `src/corrections.ts` | caught | 1 failing |
| `watermark_never_advances` | `src/reduce.ts` | caught | 3 failing |
| `canonical_json_keeps_key_order` | `src/state.ts` | caught | 1 failing |
| `engine_always_auto_accepts` | `src/acceptance.ts` | caught | 91 failing |

## What each mutant is for, and what actually went red

- **`boundary_parse_dropped_append`** — r2's blocking finding, route 1: appendEvent folds whatever it is handed. Reverting to r2 lets a raw model claim with no quote through.
  - claimed catcher(s): _refuses a raw payload that smuggles an actor, naming the seam_
  - failing tests (2):
    - _leaves the state untouched, by reference, and spends nothing_
    - _refuses a raw payload that smuggles an actor, naming the seam_
- **`boundary_parse_dropped_fold`** — The same finding on the replay path: reduce/foldEvents fold unparsed rows.
  - claimed catcher(s): _refuses a raw model claim proposal that quotes nothing, and never records it_; _refuses a whitespace-only quote exactly as it refuses a missing one_; _folds the well-formed rows around a malformed one, and reports it_
  - failing tests (4):
    - _accepts a raw row that is well-formed — the boundary is a check, not a wall_
    - _folds the well-formed rows around a malformed one, and reports it_
    - _refuses a raw model claim proposal that quotes nothing, and never records it_
    - _refuses a whitespace-only quote exactly as it refuses a missing one_
- **`empty_window_reducer`** — r2 gauntlet, empty presence: `messages: []` passes an undefined-only check and the receipt is skipped, not refused.
  - claimed catcher(s): _refuses `messages: []` at the reducer, exactly as it refuses no messages_
  - failing tests (2):
    - _refuses `messages: []` at the reducer, exactly as it refuses no messages_
    - _refuses a window of messages that exist and say nothing, at the reducer_
- **`empty_window_engine`** — The same hole in the acceptance engine, which the attention panel reads through.
  - claimed catcher(s): _refuses `messages: []` at the engine, exactly as it refuses no messages_; _refuses `messages: []` at the attention panel, with a receipt for the silence_
  - failing tests (3):
    - _refuses `messages: []` at the attention panel, with a receipt for the silence_
    - _refuses `messages: []` at the engine, exactly as it refuses no messages_
    - _refuses a window of messages that exist and say nothing, at the engine_
- **`reducer_quote_gate_dropped`** — The reducer goes back to trusting the schema one layer up for the quote.
  - claimed catcher(s): _re-requires a non-empty quote at the reducer, not one layer up_; _names the missing-quote gate rather than folding it into the receipt_
  - failing tests (3):
    - _names that gate for every model-minted type, not only the two that name a person_
    - _names the missing-quote gate rather than folding it into the receipt_
    - _re-requires a non-empty quote at the reducer, not one layer up_
- **`validator_quote_requirement_dropped`** — The validator stops requiring a quote of a model reading that names somebody.
  - claimed catcher(s): _does not let a padded citation list plus an empty quote flip attribution_; _covers every problem kind it declares_; _requires a quote from a model reading of every type, not only the two that name a person_
  - failing tests (5):
    - _covers every problem kind it declares_
    - _does not let a padded citation list plus an empty quote flip attribution_
    - _does not police attribution on types that name nobody — but still requires their receipt_
    - _refuses a model objective minted with no quote against a window of empty bodies_
    - _requires a quote from a model reading of every type, not only the two that name a person_
- **`attribution_fallback_restored`** — r2's citation-list fallback comes back: with no bearing message, any cited author supports the attribution. This is the padding attack.
  - claimed catcher(s): _does not let a padded citation list plus an empty quote flip attribution_; _rejects a quote elided with an ellipsis that is not verbatim anywhere_
  - failing tests (5):
    - _cannot be helped by dropping the citation that carries the quote_
    - _does not let a padded citation list plus an empty quote flip attribution_
    - _refuses the quote a model produced by dropping the emphasis_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _reports a plain missing quote separately from an elided one_
- **`quote_bearing_disabled`** — r2 gauntlet, major 1: the quote no longer has to carry the sentence being minted. Cite Bob's 'yes', mint 'Bob will deploy'.
  - claimed catcher(s): _refuses a verbatim, correctly attributed quote that carries a different sentence_; _refuses the same thing at the reducer, where it is a trust boundary_; _covers every problem kind it declares_; _refuses the exact pair from the gauntlet at the acceptance engine_
  - failing tests (41):
    - _covers every problem kind it declares_
    - _covers the same space the unsharded loop did, shard by shard_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _does not tell a room the quote lacks words the quote has_
    - _keeps the verdict, which was never the defect_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _names the difference in a form a reader can act on_
    - _never refuses a bearing check without naming the difference (quote insert "  ")_
    - _never refuses a bearing check without naming the difference (quote insert " ")_
    - _never refuses a bearing check without naming the difference (quote insert "'")_
    - _never refuses a bearing check without naming the difference (quote insert ",")_
    - _never refuses a bearing check without naming the difference (quote insert ".")_
    - _never refuses a bearing check without naming the difference (quote insert "?")_
    - _never refuses a bearing check without naming the difference (quote insert "\n")_
    - _never refuses a bearing check without naming the difference (quote insert "\t")_
    - _never refuses a bearing check without naming the difference (quote insert "\u0001")_
    - _never refuses a bearing check without naming the difference (quote insert "`")_
    - _never refuses a bearing check without naming the difference (quote insert "a")_
    - _never refuses a bearing check without naming the difference (quote insert "​")_
    - _never refuses a bearing check without naming the difference (quote insert "‮")_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refers a paraphrase of the quote to a person instead of accepting it_
    - _refers an inverted receipt that sits below theta_min_
    - _refers it rather than rejecting it_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a model reading with a real quote and no statement to check it against_
    - _refuses a verbatim, correctly attributed quote that carries a different sentence_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _refuses the same thing at the reducer, where it is a trust boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _reports the missing statement once, not twice_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
    - _still names a word that really is absent, so this is not "never say absent"_
    - _still rejects the two that are genuinely malformed_
- **`min_quote_length_unenforced`** — minQuoteLength goes back to being a blockquote-detection knob only.
  - claimed catcher(s): _enforces minQuoteLength on the acceptance path, not only on blockquotes_; _covers every problem kind it declares_
  - failing tests (2):
    - _covers every problem kind it declares_
    - _enforces minQuoteLength on the acceptance path, not only on blockquotes_
- **`ambiguous_quote_takes_the_first`** — Several cited authors carry the quote and the first in window order wins — attribution decided by ingestion order.
  - claimed catcher(s): _refuses as ambiguous when two cited authors both carry the quote_; _covers every problem kind it declares_
  - failing tests (2):
    - _covers every problem kind it declares_
    - _refuses as ambiguous when two cited authors both carry the quote_
- **`bearing_message_takes_the_first`** — The same defect one level down: bearingMessage answers 'who said this' with whoever is first.
  - claimed catcher(s): _reports every bearing message, and names one only when one person wrote it_; _reads an undetermined author as third-party, never as self_
  - failing tests (2):
    - _reads an undetermined author as third-party, never as self_
    - _reports every bearing message, and names one only when one person wrote it_
- **`receipt_policy_min_quote_lowered`** — The pinned receipt minimum moves. A pin that is derived rather than literal would not notice.
  - claimed catcher(s): _is exactly this table_; _is the number the escalation config quotes by, not a second one_; _enforces minQuoteLength on the acceptance path, not only on blockquotes_
  - failing tests (5):
    - _covers every problem kind it declares_
    - _enforces minQuoteLength on the acceptance path, not only on blockquotes_
    - _is exactly this table_
    - _is the number the escalation config quotes by, not a second one_
    - _puts the quote-length line exactly where the number says_
- **`dedup_ignores_polarity`** — Puts the stopword-filtered similarity score back in `findDuplicate`. A duplicate is discarded, so the score decides what the room never sees — and `not`, `all` and `some` are stopwords, which made a contradiction of an accepted object score 1.0 against it.
  - claimed catcher(s): _does not discard a reading that says the opposite of the accepted one_
  - failing tests (7):
    - _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
    - _discards only a re-proposal of the same sentence_
    - _does not discard a quantifier substitution either_
    - _does not discard a reading that says the opposite of the accepted one_
    - _does not discard the negation of an accepted claim as a re-proposal of it_
    - _keeps `10² ms` and `102 ms` apart_
    - _r9 — keeps a re-cased literal apart from the literal it is not_
- **`claim_theta_auto_lowered`** — r1's original escape: the θ table moves and every probe derived from it moves with it. The literal probes must catch this.
  - claimed catcher(s): _is exactly this table_; _does not accept a hair under θ_auto (0.6999)_; _is the engine’s θ, not a second number under it_
  - failing tests (12):
    - _claim in the θ band_
    - _discards a reading the table says to discard rather than surfacing it_
    - _does not accept a hair under θ_auto (0.6999)_
    - _has the ordering #4 argues for — the cost of being wrong sets the bar_
    - _is derived from the θ table, entry by entry_
    - _is exactly this table_
    - _is the engine’s θ, not a second number under it_
    - _is the same number the reducer folds against — one table, not two_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _model_proposer accepts claim via model_a @below floor_
    - _places fixed confidences in the cells the table implies_
    - _probes the band each literal claims to be in_
- **`claim_theta_min_lowered`** — The other end of the band, likewise.
  - claimed catcher(s): _is exactly this table_; _discards a hair under θ_min (0.4999)_
  - failing tests (8):
    - _claim below θ_min_
    - _discards a hair under θ_min (0.4999)_
    - _discards a reading the table says to discard rather than surfacing it_
    - _is exactly this table_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _places fixed confidences in the cells the table implies_
    - _probes the band each literal claims to be in_
    - _still discards a weak reading whose receipt is clean_
- **`floor_decoupled_from_theta`** — The reducer's floor goes back to being a second, lower set of numbers than the engine's θ.
  - claimed catcher(s): _is the same number the reducer folds against — one table, not two_; _is the engine’s θ, not a second number under it_
  - failing tests (6):
    - _is derived from the θ table, entry by entry_
    - _is the engine’s θ, not a second number under it_
    - _is the same number the reducer folds against — one table, not two_
    - _model_proposer accepts claim via model_a @below floor_
    - _model_proposer accepts open_question via model_a @below floor_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
- **`type_certification_removed`** — r7. Deletes the engine's refusal to auto-accept a reading whose words are an undertaking as easily as an assertion, so `type` — a model-supplied field — again selects the rule that judges the proposal. One body, one quote, one author, confidence 0.95: as a `commitment` it is `pending / never_auto_accepts`, as a `claim` it is `auto_accept`.
  - claimed catcher(s): _refers a claim whose words are equally an undertaking_; _refuses the laundered commitment at the engine_
  - failing tests (19):
    - _accepts the same reading once the quote is the whole sentence_
    - _accepts the same shape once the quote carries the sentence_
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _drives every needs-you-capable rule through a real staged proposal_
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _never emits an acceptance the reducer would refuse to fold_
    - _places fixed confidences in the cells the table implies_
    - _raises an item or refuses, for every one of them — with a member list_
    - _raises an item or refuses, for every one of them — without a member list_
    - _reaches every rule name the type declares_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _reads the plain ways of undertaking something, not only the hedged ones_
    - _refers a claim whose words are equally an undertaking_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses the laundered commitment at the engine_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _renders the laundered reading the engine sent to Needs-you_
    - _stops firing on four sentences that undertake nothing_
- **`claim_certification_always_true`** — r7. Says every claim is certifiable whatever its words, which is r6 exactly: the laundered commitment goes back to auto-accepting as a claim.
  - claimed catcher(s): _refers a claim whose words are equally an undertaking_; _refuses the laundered commitment at the engine_; _refuses the laundered commitment at the reducer, where it is a boundary_
  - failing tests (26):
    - _accepts the same reading once the quote is the whole sentence_
    - _accepts the same shape once the quote carries the sentence_
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _drives every needs-you-capable rule through a real staged proposal_
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _model_proposer accepts a claim with a uncertified_type receipt_
    - _never emits an acceptance the reducer would refuse to fold_
    - _places fixed confidences in the cells the table implies_
    - _raises an item or refuses, for every one of them — with a member list_
    - _raises an item or refuses, for every one of them — without a member list_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _reaches every rule name the type declares_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _reads the plain ways of undertaking something, not only the hedged ones_
    - _refers a claim whose words are equally an undertaking_
    - _refuses a commitment-shaped claim at any confidence, above the floor included_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses a model landing a claim whose words could be an undertaking_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the laundered commitment at the engine_
    - _refuses the laundered commitment at the reducer, where it is a boundary_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _renders the laundered reading the engine sent to Needs-you_
    - _stops firing on four sentences that undertake nothing_
- **`declarative_open_question_allowed`** — r7's mirror. Deletes the refusal of a declarative sentence minted as an `open_question` — the type with the lowest θ_auto in the table (0.6 against a claim's 0.7), so a reading routed through it clears a bar its own type would not.
  - claimed catcher(s): _covers every problem kind it declares_; _does not police attribution on types that name nobody — but still requires their receipt_
  - failing tests (3):
    - _covers every problem kind it declares_
    - _does not police attribution on types that name nobody — but still requires their receipt_
    - _refuses an assertion on an unverifiable mark without certifying a question_
- **`payload_actor_guard_disabled`** — The event schema stops refusing a payload-supplied actor.
  - claimed catcher(s): _refuses a raw payload that smuggles an actor, naming the seam_
  - failing tests (4):
    - _refuses a forged actor on every event type_
    - _refuses a payload that carries one, rather than stripping it silently_
    - _refuses a raw payload that smuggles an actor, naming the seam_
    - _will not type-check either, which is the half that catches it before runtime_
- **`receipt_skipped_entirely`** — The reducer stops running the receipt on non-human acceptances at all — r1's blocking finding.
  - claimed catcher(s): _refuses a model acceptance with no message window at all_; _refuses one whose quote is only inside somebody else’s blockquote_; _refuses `messages: []` at the reducer, exactly as it refuses no messages_
  - failing tests (20):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_proposer accepts a claim with a citations receipt_
    - _model_proposer accepts a claim with a no_window receipt_
    - _model_proposer accepts a claim with a payload receipt_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _model_proposer accepts a claim with a wrong_author receipt_
    - _re-requires a non-empty quote at the reducer, not one layer up_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses `messages: []` at the reducer, exactly as it refuses no messages_
    - _refuses a claim whose claimant wrote nothing it cites_
    - _refuses a model acceptance with no message window at all_
    - _refuses a window of messages that exist and say nothing, at the reducer_
    - _refuses an acceptance that pads or drops cited messages on the way through_
    - _refuses an acceptance whose payload is not the one that was staged_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses one whose quote is only inside somebody else’s blockquote_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _refuses the padded window at the reducer, where it is a boundary_
    - _refuses the same thing at the reducer, where it is a trust boundary_
    - _reports the receipt fault rather than the confidence, when both hold_
- **`payload_binding_disabled`** — A machine may mint a payload nobody staged behind a citation that says a person could have checked it.
  - claimed catcher(s): _refuses an acceptance whose payload is not the one that was staged_
  - failing tests (4):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_proposer accepts a claim with a payload receipt_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses an acceptance whose payload is not the one that was staged_
- **`provenance_binding_disabled`** — The citation set may change on the way through acceptance — which is what the attribution rules are computed over.
  - claimed catcher(s): _refuses an acceptance that pads or drops cited messages on the way through_
  - failing tests (4):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_proposer accepts a claim with a citations receipt_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses an acceptance that pads or drops cited messages on the way through_
- **`proposer_binding_disabled`** — One interpreter may accept, reject or retire another's staged readings.
  - claimed catcher(s): _refuses a second model accepting the first model’s proposal_; _refuses a model accepting a human’s proposal_
  - failing tests (48):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_other accepts a claim with a citations receipt_
    - _model_other accepts a claim with a faithful receipt_
    - _model_other accepts a claim with a no_window receipt_
    - _model_other accepts a claim with a payload receipt_
    - _model_other accepts a claim with a uncertifiable receipt_
    - _model_other accepts a claim with a uncertified_type receipt_
    - _model_other accepts a claim with a wrong_author receipt_
    - _model_other accepts claim via human @above floor_
    - _model_other accepts claim via human @below floor_
    - _model_other accepts claim via model_a @above floor_
    - _model_other accepts claim via model_a @below floor_
    - _model_other accepts open_question via human @above floor_
    - _model_other accepts open_question via human @below floor_
    - _model_other accepts open_question via model_a @above floor_
    - _model_other accepts open_question via model_a @below floor_
    - _model_other rejects a human proposal recorded by human_
    - _model_other rejects a human proposal recorded by model_other_
    - _model_other rejects a human proposal recorded by model_proposer_
    - _model_other rejects a human proposal recorded by system_
    - _model_other rejects a model_a proposal recorded by human_
    - _model_other rejects a model_a proposal recorded by model_other_
    - _model_other rejects a model_a proposal recorded by model_proposer_
    - _model_other rejects a model_a proposal recorded by system_
    - _model_other supersedes a human proposal recorded by human_
    - _model_other supersedes a human proposal recorded by model_other_
    - _model_other supersedes a human proposal recorded by model_proposer_
    - _model_other supersedes a human proposal recorded by system_
    - _model_other supersedes a model_a proposal recorded by human_
    - _model_other supersedes a model_a proposal recorded by model_other_
    - _model_other supersedes a model_a proposal recorded by model_proposer_
    - _model_other supersedes a model_a proposal recorded by system_
    - _model_proposer accepts claim via human @above floor_
    - _model_proposer accepts claim via human @below floor_
    - _model_proposer accepts open_question via human @above floor_
    - _model_proposer accepts open_question via human @below floor_
    - _model_proposer rejects a human proposal recorded by human_
    - _model_proposer rejects a human proposal recorded by model_other_
    - _model_proposer rejects a human proposal recorded by model_proposer_
    - _model_proposer rejects a human proposal recorded by system_
    - _model_proposer supersedes a human proposal recorded by human_
    - _model_proposer supersedes a human proposal recorded by model_other_
    - _model_proposer supersedes a human proposal recorded by model_proposer_
    - _model_proposer supersedes a human proposal recorded by system_
    - _refuses a model accepting a human’s proposal_
    - _refuses a model rejecting a human’s proposal, but lets a human reject a model’s_
    - _refuses a second model accepting the first model’s proposal_
    - _refuses a second model rejecting or superseding another’s reading_
- **`confidence_floor_disabled`** — A machine may mint an object from a reading it does not stand behind, at any confidence including zero.
  - claimed catcher(s): _refuses a model accepting its own near-zero-confidence claim_; _is the engine’s θ, not a second number under it_
  - failing tests (6):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _is the engine’s θ, not a second number under it_
    - _model_proposer accepts claim via model_a @below floor_
    - _model_proposer accepts open_question via model_a @below floor_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model accepting its own near-zero-confidence claim_
- **`decision_never_auto_disabled`** — #4's one absolute: a decision never auto-accepts, at any confidence.
  - claimed catcher(s): _refuses a model actor accepting a decision through its own proposal_; _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
  - failing tests (53):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_other accepts commitment via human @above floor_
    - _model_other accepts commitment via human @below floor_
    - _model_other accepts commitment via model_a @above floor_
    - _model_other accepts commitment via model_a @below floor_
    - _model_other accepts commitment via none_
    - _model_other accepts decision via human @above floor_
    - _model_other accepts decision via human @below floor_
    - _model_other accepts decision via model_a @above floor_
    - _model_other accepts decision via model_a @below floor_
    - _model_other accepts decision via none_
    - _model_other accepts objective via human @above floor_
    - _model_other accepts objective via human @below floor_
    - _model_other accepts objective via model_a @above floor_
    - _model_other accepts objective via model_a @below floor_
    - _model_other accepts objective via none_
    - _model_proposer accepts commitment via human @above floor_
    - _model_proposer accepts commitment via human @below floor_
    - _model_proposer accepts commitment via model_a @above floor_
    - _model_proposer accepts commitment via model_a @below floor_
    - _model_proposer accepts commitment via none_
    - _model_proposer accepts decision via human @above floor_
    - _model_proposer accepts decision via human @below floor_
    - _model_proposer accepts decision via model_a @above floor_
    - _model_proposer accepts decision via model_a @below floor_
    - _model_proposer accepts decision via none_
    - _model_proposer accepts objective via human @above floor_
    - _model_proposer accepts objective via human @below floor_
    - _model_proposer accepts objective via model_a @above floor_
    - _model_proposer accepts objective via model_a @below floor_
    - _model_proposer accepts objective via none_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model accepting a commitment before the receipt is even read_
    - _refuses a model accepting a commitment somebody else’s sentence named_
    - _refuses a model accepting an objective — the heading everything is filed under_
    - _refuses a model actor accepting a decision through its own proposal_
    - _refuses a system actor accepting a decision too_
    - _refuses it even when the owner wrote the sentence himself_
    - _system accepts commitment via human @above floor_
    - _system accepts commitment via human @below floor_
    - _system accepts commitment via model_a @above floor_
    - _system accepts commitment via model_a @below floor_
    - _system accepts commitment via none_
    - _system accepts decision via human @above floor_
    - _system accepts decision via human @below floor_
    - _system accepts decision via model_a @above floor_
    - _system accepts decision via model_a @below floor_
    - _system accepts decision via none_
    - _system accepts objective via human @above floor_
    - _system accepts objective via human @below floor_
    - _system accepts objective via model_a @above floor_
    - _system accepts objective via model_a @below floor_
    - _system accepts objective via none_
- **`claim_verification_gate_disabled`** — `~` becomes `✓` on a machine's word.
  - claimed catcher(s): _refuses a claim born verified on a model actor’s word_
  - failing tests (17):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_other accepts claim (verified) via human @above floor_
    - _model_other accepts claim (verified) via human @below floor_
    - _model_other accepts claim (verified) via model_a @above floor_
    - _model_other accepts claim (verified) via model_a @below floor_
    - _model_other accepts claim (verified) via none_
    - _model_proposer accepts claim (verified) via human @above floor_
    - _model_proposer accepts claim (verified) via human @below floor_
    - _model_proposer accepts claim (verified) via model_a @above floor_
    - _model_proposer accepts claim (verified) via model_a @below floor_
    - _model_proposer accepts claim (verified) via none_
    - _refuses a claim born verified on a model actor’s word_
    - _system accepts claim (verified) via human @above floor_
    - _system accepts claim (verified) via human @below floor_
    - _system accepts claim (verified) via model_a @above floor_
    - _system accepts claim (verified) via model_a @below floor_
    - _system accepts claim (verified) via none_
- **`direct_acceptance_gate_disabled`** — A machine may skip the proposal entirely and emit the object — the whole acceptance boundary becomes optional.
  - claimed catcher(s): _refuses a proposal-less acceptance from a model actor_; _refuses a proposal-less acceptance from a system actor too_
  - failing tests (14):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _folds the same rows to a different state under a different actor_
    - _gates on the acceptance actor, not on who proposed_
    - _lets a model accept a disputed claim — only ✓ is gated_
    - _model_other accepts claim via none_
    - _model_other accepts open_question via none_
    - _model_proposer accepts claim via none_
    - _model_proposer accepts open_question via none_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a proposal-less acceptance from a model actor_
    - _refuses a proposal-less acceptance from a system actor too_
    - _still lets a model accept the same claim unverified — the `~` path_
    - _system accepts claim via none_
    - _system accepts open_question via none_
- **`answers_human_gate_disabled`** — A machine may close a person's open question by pointing an arbitrary claim at it.
  - claimed catcher(s): _refuses a model answering an open question_
  - failing tests (6):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_other adds "answers"_
    - _model_proposer adds "answers"_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model answering an open question_
    - _system adds "answers"_
- **`supersession_policy_narrowed`** — Supersession goes back to gating decisions only, so a machine may retire a human-accepted commitment.
  - claimed catcher(s): _refuses a model retiring a commitment_; _refuses a model retiring an objective_
  - failing tests (8):
    - _model_other adds "supersedes" retiring a commitment_
    - _model_other adds "supersedes" retiring a objective_
    - _model_proposer adds "supersedes" retiring a commitment_
    - _model_proposer adds "supersedes" retiring a objective_
    - _refuses a model retiring a commitment_
    - _refuses a model retiring an objective_
    - _system adds "supersedes" retiring a commitment_
    - _system adds "supersedes" retiring a objective_
- **`corrections_human_gate_disabled`** — Every correction verb is human-only in v1; a correction reaches backwards.
  - claimed catcher(s): _refuses a system actor correction as well_
  - failing tests (27):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_other performs "amend"_
    - _model_other performs "reattribute"_
    - _model_other performs "reopen"_
    - _model_other performs "restore"_
    - _model_other performs "retract"_
    - _model_other performs "retype"_
    - _model_proposer performs "amend"_
    - _model_proposer performs "reattribute"_
    - _model_proposer performs "reopen"_
    - _model_proposer performs "restore"_
    - _model_proposer performs "retract"_
    - _model_proposer performs "retype"_
    - _names the verification rule when a model’s correction would produce a ✓_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model "amend"_
    - _refuses a model "restore"_
    - _refuses a model "retract"_
    - _refuses a model amending a claim to verified, naming the verification rule_
    - _refuses a retype from a model, like every other correction verb_
    - _refuses a system actor correction as well_
    - _system performs "amend"_
    - _system performs "reattribute"_
    - _system performs "reopen"_
    - _system performs "restore"_
    - _system performs "retract"_
    - _system performs "retype"_
- **`blockquote_stripping_neutered`** — The spike's worst error: a quote that exists only inside somebody else's reply-blockquote.
  - claimed catcher(s): _refuses one whose quote is only inside somebody else’s blockquote_
  - failing tests (12):
    - _catches the worst error in the whole spike_
    - _covers every problem kind it declares_
    - _discards a 0.98-confidence claim whose receipt is wrong_
    - _does not count a message that was only quoting it_
    - _does not count a sentence the cited author only quoted back_
    - _marks every wrong-receipt problem as rejecting_
    - _reads only the author’s own text, not what they quoted_
    - _refers the same claim when its author wrote more around the sentence_
    - _refuses one whose quote is only inside somebody else’s blockquote_
    - _removes nested quotes too — they are quoting harder, not less_
    - _removes quoted lines and keeps the author’s own text_
    - _reports every bearing message, and names one only when one person wrote it_
- **`answer_binding_order_check_dropped`** — The two answer-binding events share a timestamp; if the relation sorts first the question is left open beside its own answer.
  - claimed catcher(s): _refuses ids whose order would put the edge before the object_
  - failing tests (1):
    - _refuses ids whose order would put the edge before the object_
- **`answer_binding_question_open_check_dropped`** — Binding a second answer leaves two `answers` edges and one status.
  - claimed catcher(s): _refuses a question that is already answered_
  - failing tests (1):
    - _refuses a question that is already answered_
- **`attention_reads_its_own_theta`** — The panel and the engine disagree about a proposal in the θ band — r1's major 9.
  - claimed catcher(s): _stays quiet about a decision the engine would not surface_
  - failing tests (3):
    - _does not raise one for a reading the engine accepted_
    - _stays quiet about a commitment the engine discarded_
    - _stays quiet about a decision the engine would not surface_
- **`attention_priority_tiers_swapped`** — The hardest-first sort is the whole product of the panel.
  - claimed catcher(s): _gives every class a tier, and every tier a distinct rank_
  - failing tests (4):
    - _follows #6’s order exactly_
    - _gives every class a tier, and every tier a distinct rank_
    - _sorts by those ranks, in #6’s stated order_
    - _sorts stored items that carry no priority, by class_
- **`ordering_gate_loosened`** — An event that lands exactly on the cursor is admitted, so two payloads can claim one position.
  - claimed catcher(s): _rejects an event that lands on exactly the cursor_
  - failing tests (3):
    - _rejects a verbatim redelivery, also without a trace_
    - _rejects an event that lands on exactly the cursor_
    - _reports each redelivery as a rejection rather than swallowing it_
- **`consumed_ids_not_spent_on_failure`** — A redelivery of an event that failed its business checks gets a second try against a state that has moved on.
  - claimed catcher(s): _spends the id of an event that was consumed but did not apply_
  - failing tests (35):
    - _exercised every outcome, so the equality above is not vacuous_
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 1)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 13)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 144)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 2)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 21)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 233)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 3)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 34)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 5)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 55)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 8)_
    - _folds a jittered 60-event log to the replay the oracle reconstructs (seed 89)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 1)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 13)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 144)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 2)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 21)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 233)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 3)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 34)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 5)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 55)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 8)_
    - _folds a shuffled 60-event log to the replay the oracle reconstructs (seed 89)_
    - _records an issue for an unknown proposal rather than throwing_
    - _records an issue instead of throwing on an unknown object_
    - _refuses a model "amend"_
    - _refuses a model "restore"_
    - _refuses a model "retract"_
    - _refuses a model actor accepting a decision through its own proposal_
    - _refuses a proposal-less acceptance from a model actor_
    - _refuses an acceptance citing a proposal that does not exist_
    - _spends the id of an event that was consumed but did not apply_
- **`amend_may_touch_attribution`** — Moving an obligation off a named person becomes a substring search over patches instead of a verb.
  - claimed catcher(s): _refuses an amend that would move the obligation instead_
  - failing tests (1):
    - _refuses an amend that would move the obligation instead_
- **`noop_amend_silent_again`** — An amendment that changes nothing reports `applied` with nothing applied.
  - claimed catcher(s): _treats an empty amend patch as a no-op — and says so rather than reporting success_; _folds the parsed value, not the raw one — so schema defaults are real_
  - failing tests (3):
    - _folds the parsed value, not the raw one — so schema defaults are real_
    - _treats a patch that changes nothing as a no-op too_
    - _treats an empty amend patch as a no-op — and says so rather than reporting success_
- **`one_concession_marker_deleted`** — A table driven from a constant cannot notice a row that stopped existing; the by-value pin must.
  - claimed catcher(s): _is exactly this list_
  - failing tests (1):
    - _is exactly this list_
- **`one_future_pattern_deleted`** — Same, for the future-tense patterns.
  - claimed catcher(s): _is exactly these five patterns_; _has a case for every pattern_
  - failing tests (3):
    - _has a case for every pattern_
    - _is exactly these five patterns_
    - _pattern 4 fires on "please review the patch before Friday"_
- **`escalation_scan_cap_loosened`** — The per-call cost of the pre-model pass goes back to being chosen by whoever writes the messages.
  - claimed catcher(s): _bounds the work per call, so content cannot choose the cost_
  - failing tests (1):
    - _bounds the work per call, so content cannot choose the cost_
- **`rationale_accepts_a_bare_string`** — The attention rationale brand stops binding at runtime, so a store can hand back a sentence nobody's template produced.
  - claimed catcher(s): _requires a structured reason — no unexplained attention, and no free text_
  - failing tests (2):
    - _renders its sentence from the reason, naming the person first_
    - _requires a structured reason — no unexplained attention, and no free text_
- **`subject_kind_default_restored`** — An attention item that silently defaults to 'object' points #22's foreign key at a proposal id.
  - claimed catcher(s): _has no default for subjectKind — the wrong value there is unfalsifiable_
  - failing tests (1):
    - _has no default for subjectKind — the wrong value there is unfalsifiable_
- **`counterexample_voice_reverted`** — CONVENTIONS: the system never synthesizes speech. 'the room said' attributes a form field to people.
  - claimed catcher(s): _formats a block ready to inject, quoting the note as a note_
  - failing tests (1):
    - _formats a block ready to inject, quoting the note as a note_
- **`blank_window_reducer`** — r3 gauntlet, major 2: a window of messages whose bodies are all empty is structurally non-empty, so `length === 0` is false and nothing downstream has anything to find a fault in.
  - claimed catcher(s): _refuses a window of messages that exist and say nothing, at the reducer_
  - failing tests (1):
    - _refuses a window of messages that exist and say nothing, at the reducer_
- **`blank_window_engine`** — The same hole at the acceptance engine, which the attention panel reads through.
  - claimed catcher(s): _refuses a window of messages that exist and say nothing, at the engine_
  - failing tests (1):
    - _refuses a window of messages that exist and say nothing, at the engine_
- **`reducer_quote_gate_rescoped`** — r3 gauntlet, major 2: the reducer's missing-quote gate goes back to covering only the two types that name a person, which is the scope a model objective was minted through.
  - claimed catcher(s): _names that gate for every model-minted type, not only the two that name a person_
  - failing tests (1):
    - _names that gate for every model-minted type, not only the two that name a person_
- **`validator_quote_requirement_rescoped`** — The same rescoping in the validator both the engine and the reducer run.
  - claimed catcher(s): _requires a quote from a model reading of every type, not only the two that name a person_; _refuses a model objective minted with no quote against a window of empty bodies_; _does not police attribution on types that name nobody — but still requires their receipt_
  - failing tests (3):
    - _does not police attribution on types that name nobody — but still requires their receipt_
    - _refuses a model objective minted with no quote against a window of empty bodies_
    - _requires a quote from a model reading of every type, not only the two that name a person_
- **`proposal_quote_rescoped`** — The schema stops requiring a quote from a model proposal of the three types that name nobody.
  - claimed catcher(s): _requires that quote from a model on every type, not only the two that name a person_; _refuses a model objective minted with no quote against a window of empty bodies_
  - failing tests (2):
    - _refuses a model objective minted with no quote against a window of empty bodies_
    - _requires that quote from a model on every type, not only the two that name a person_
- **`bearing_forgives_interposed_words`** — r3 gauntlet, major 1, exactly: a word the quote carries and the statement drops stops being reported, so `Bob will not deploy production Friday` supports `Bob will deploy production Friday`.
  - claimed catcher(s): _refuses the exact pair from the gauntlet at the acceptance engine_; _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_; _names the interposed word rather than scrambling the rest of the alignment_; _refuses negation_
  - failing tests (89):
    - _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
    - _accepts each of them when the statement really is the quote_
    - _accepts exactly at θ_auto (0.7 for a claim)_
    - _accepts the same reading once the quote is the whole sentence_
    - _asks the named owner to confirm a third-party commitment_
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _cannot be helped by dropping the citation that carries the quote_
    - _cannot be made to collide across the proposal and object namespaces_
    - _catches the worst error in the whole spike_
    - _certifies the same sentence when it is the whole of what the author wrote_
    - _claim at θ_auto_
    - _claim below θ_min_
    - _claim in the θ band_
    - _commitment (self) at θ_auto_
    - _commitment (self) below θ_min_
    - _commitment (self) in the θ band_
    - _commitment (third-party) at θ_auto_
    - _commitment (third_party) below θ_min_
    - _commitment (third_party) in the θ band_
    - _covers every problem kind it declares_
    - _decision at confidence 1.0_
    - _decision at θ_auto_
    - _decision below θ_min_
    - _decision in the θ band_
    - _discards a hair under θ_min (0.4999)_
    - _discards a re-proposal of something already accepted_
    - _does not accept a hair under θ_auto (0.6999)_
    - _does not certify a quote whose message carries a trailing inverter_
    - _does not fire on the conversational half of the concession list_
    - _does not let a padded citation list flip attribution_
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _does not police attribution on types that name nobody — but still requires their receipt_
    - _does not tell a room the quote lacks words the quote has_
    - _drives every needs-you-capable rule through a real staged proposal_
    - _fans an unassigned decision out to every member — "or any-member when unassigned"_
    - _finds a quote reproduced exactly, and reports only the scissors_
    - _follows #6’s order exactly_
    - _is a statement the receipt genuinely certifies_
    - _is a tagged union, so the three answers cannot be told apart by truthiness_
    - _lands a model claim at the floor when the words certify the type_
    - _leaves an unrelated later message alone_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _lets a genuinely later window through, so this is not "refuse every window"_
    - _lets the ordinary window through, so this is not "refuse every window"_
    - _names the difference in a form a reader can act on_
    - _names the interposed word rather than scrambling the rest of the alignment_
    - _never refuses a message quoted as itself_
    - _objective at θ_auto_
    - _objective below θ_min_
    - _objective in the θ band_
    - _passes the claim when the quote is the whole of what that author wrote_
    - _places fixed confidences in the cells the table implies_
    - _reaches every rule name the type declares_
    - _reads a line break the way every renderer does, not the way a newline does_
    - _refers a claim whose words are equally an undertaking_
    - _refers a leading sentence too — the scissors cut both ways_
    - _refers a quote whose author put the inverter in their own blockquote_
    - _refers an inverted receipt that sits below theta_min_
    - _refers every other way a neighbouring sentence changes the force_
    - _refers it rather than rejecting it_
    - _refers the same claim when its author wrote more around the sentence_
    - _refuses "no longer"_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses a leading hedge the statement drops_
    - _refuses a modal flip from must not to must_
    - _refuses a negation written in another script or another width_
    - _refuses a symbol that carries the negation_
    - _refuses an article substitution, which laundered an indefinite reference_
    - _refuses an unassigned decision it has nobody to show, and names the room_
    - _refuses double negation_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses negation_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _refuses the quote a model produced by dropping the emphasis_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _renders the laundered reading the engine sent to Needs-you_
    - _requires a commitment’s owner to have authored a cited message_
    - _routes a named decision to the person named_
    - _says how far it read, so "found nothing" is not "read nothing"_
    - _says it read nothing, rather than reporting a clean scan of nothing_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
    - _stays quiet about a commitment the engine discarded_
    - _still discards a weak reading whose receipt is clean_
    - _still names a word that really is absent, so this is not "never say absent"_
    - _stops firing on four sentences that undertake nothing_
- **`bearing_forgives_missing_statement_words`** — The other direction: the statement may assert words the quote does not contain, which is the quantifier flip (all -> some) and the invented sentence.
  - claimed catcher(s): _refuses a statement that inserts a word the quote does not contain_; _refuses a verbatim, correctly attributed quote that carries a different sentence_
  - failing tests (114):
    - _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
    - _accepts a raw row that is well-formed — the boundary is a check, not a wall_
    - _accepts each of them when the statement really is the quote_
    - _accepts exactly at θ_auto (0.7 for a claim)_
    - _accepts the same reading once the quote is the whole sentence_
    - _accepts the same shape once the quote carries the sentence_
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _asks the named owner to confirm a third-party commitment_
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _cannot be helped by dropping the citation that carries the quote_
    - _cannot be made to collide across the proposal and object namespaces_
    - _catches the worst error in the whole spike_
    - _certifies the same sentence when it is the whole of what the author wrote_
    - _claim at θ_auto_
    - _claim below θ_min_
    - _claim in the θ band_
    - _commitment (self) at θ_auto_
    - _commitment (self) below θ_min_
    - _commitment (self) in the θ band_
    - _commitment (third-party) at θ_auto_
    - _commitment (third_party) below θ_min_
    - _commitment (third_party) in the θ band_
    - _covers every problem kind it declares_
    - _decision at confidence 1.0_
    - _decision at θ_auto_
    - _decision below θ_min_
    - _decision in the θ band_
    - _discards a hair under θ_min (0.4999)_
    - _discards a re-proposal of something already accepted_
    - _discards a reading the table says to discard rather than surfacing it_
    - _does not accept a hair under θ_auto (0.6999)_
    - _does not certify a quote whose message carries a trailing inverter_
    - _does not fire on the conversational half of the concession list_
    - _does not let a padded citation list flip attribution_
    - _does not mint a question as a claim_
    - _does not refuse the same person saying it twice — the answer is determined_
    - _does not tell a room the quote lacks words the quote has_
    - _drives every needs-you-capable rule through a real staged proposal_
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _fans an unassigned decision out to every member — "or any-member when unassigned"_
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _follows #6’s order exactly_
    - _is a statement the receipt genuinely certifies_
    - _is a tagged union, so the three answers cannot be told apart by truthiness_
    - _is confirmed the moment a person corrects it_
    - _is the engine’s θ, not a second number under it_
    - _is unconfirmed for a model-accepted object_
    - _keeps auto-accepting an unambiguous assertion — this is the path, not a cost_
    - _keeps the third-party receipt gate live under the row that now hides it_
    - _keeps the verdict, which was never the defect_
    - _lands a model claim at the floor when the words certify the type_
    - _leaves an unrelated later message alone_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _lets a genuinely later window through, so this is not "refuse every window"_
    - _lets a model actor accept its own claim proposal — the route that stays open_
    - _lets the ordinary window through, so this is not "refuse every window"_
    - _lets the same acceptance through once it clears the floor_
    - _marks every wrong-receipt problem as rejecting_
    - _mints the same sentence as an open question_
    - _model_proposer accepts a claim with a faithful receipt_
    - _model_proposer accepts a claim with a uncertified_type receipt_
    - _model_proposer accepts claim via model_a @above floor_
    - _model_proposer accepts claim via model_a @below floor_
    - _model_proposer accepts open_question via model_a @above floor_
    - _model_proposer accepts open_question via model_a @below floor_
    - _names the difference in a form a reader can act on_
    - _names the residue rather than implying it is covered_
    - _never refuses a message quoted as itself_
    - _objective at θ_auto_
    - _objective below θ_min_
    - _objective in the θ band_
    - _open_question at θ_auto_
    - _open_question below θ_min_
    - _open_question in the θ band_
    - _passes the claim when the quote is the whole of what that author wrote_
    - _places fixed confidences in the cells the table implies_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _reaches every rule name the type declares_
    - _reads a line break the way every renderer does, not the way a newline does_
    - _reads the plain ways of undertaking something, not only the hedged ones_
    - _refers a claim whose words are equally an undertaking_
    - _refers a leading sentence too — the scissors cut both ways_
    - _refers a quote whose author put the inverter in their own blockquote_
    - _refers every other way a neighbouring sentence changes the force_
    - _refers it rather than rejecting it_
    - _refers the same claim when its author wrote more around the sentence_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a commitment-shaped claim at any confidence, above the floor included_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses a model accepting its own near-zero-confidence claim_
    - _refuses a model landing a claim whose words could be an undertaking_
    - _refuses a statement that inserts a word the quote does not contain_
    - _refuses a verbatim, correctly attributed quote that carries a different sentence_
    - _refuses an unassigned decision it has nobody to show, and names the room_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the laundered commitment at the engine_
    - _refuses the laundered commitment at the reducer, where it is a boundary_
    - _refuses the same thing at the reducer, where it is a trust boundary_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _renders the laundered reading the engine sent to Needs-you_
    - _reports a plain missing quote separately from an elided one_
    - _routes a named decision to the person named_
    - _says how far it read, so "found nothing" is not "read nothing"_
    - _says it read nothing, rather than reporting a clean scan of nothing_
    - _says when it became a fact, or that it is not one yet_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
    - _stays quiet about a commitment the engine discarded_
    - _still discards a weak reading whose receipt is clean_
    - _still lets a model accept the same claim unverified — the `~` path_
    - _still names a word that really is absent, so this is not "never say absent"_
    - _stops firing on four sentences that undertake nothing_
- **`bearing_forgives_the_tail`** — Only the covering span of the statement is checked, so a trailing `unless the migration is still red` is invisible.
  - claimed catcher(s): _refuses a trailing "unless"_; _refuses a trailing "except"_; _refuses a quote that is a superset carrying an unrelated sentence_
  - failing tests (43):
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _cannot be made to collide across the proposal and object namespaces_
    - _catches the worst error in the whole spike_
    - _decision at confidence 1.0_
    - _decision at θ_auto_
    - _decision below θ_min_
    - _decision in the θ band_
    - _discards a reading the table says to discard rather than surfacing it_
    - _does not mint a question as a claim_
    - _does not refuse the same person saying it twice — the answer is determined_
    - _does not tell a room the quote lacks words the quote has_
    - _is a statement the receipt genuinely certifies_
    - _keeps the third-party receipt gate live under the row that now hides it_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _marks every wrong-receipt problem as rejecting_
    - _mints the same sentence as an open question_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _model_proposer accepts open_question via model_a @above floor_
    - _model_proposer accepts open_question via model_a @below floor_
    - _never refuses a message quoted as itself_
    - _open_question at θ_auto_
    - _open_question below θ_min_
    - _open_question in the θ band_
    - _places fixed confidences in the cells the table implies_
    - _reads the plain ways of undertaking something, not only the hedged ones_
    - _refers a claim whose words are equally an undertaking_
    - _refers the same claim when its author wrote more around the sentence_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a difference of a full stop, like every other difference_
    - _refuses a question minted as an assertion_
    - _refuses a quote that is a superset carrying an unrelated sentence_
    - _refuses a trailing "except"_
    - _refuses a trailing "unless"_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the laundered commitment at the engine_
    - _refuses the laundered commitment at the reducer, where it is a boundary_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _renders the laundered reading the engine sent to Needs-you_
    - _reports a plain missing quote separately from an elided one_
    - _reports the receipt fault rather than the confidence, when both hold_
    - _says it read nothing, rather than reporting a clean scan of nothing_
    - _still names a word that really is absent, so this is not "never say absent"_
    - _stops firing on four sentences that undertake nothing_
- **`bearing_forgives_the_statement_tail`** — The statement may run past the end of the quote and assert whatever it likes there.
  - claimed catcher(s): _refuses a statement that runs past the end of its quote_
  - failing tests (1):
    - _refuses a statement that runs past the end of its quote_
- **`ordered_tokens_dedup`** — The bearing tokens become a set again, which is how r3 could not have told double negation from single even with `not` off the stopword list.
  - claimed catcher(s): _counts a repeated word twice, so double negation is not one negation_; _refuses double negation_
  - failing tests (19):
    - _accepts a run of several whole sentences, and refuses half of one_
    - _checks a sentence written in a language with no ASCII in it at all_
    - _counts a repeated word twice, so double negation is not one negation_
    - _covers every problem kind it declares_
    - _does not treat a contraction as its expansion_
    - _is borne exactly when the two normalized texts are the same string_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _keeps two-letter words, because "no" inverts a sentence_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles the normalized text exactly, for every text_
    - _refers it rather than rejecting it_
    - _refuses an input too large to align rather than approximating it_
    - _refuses double negation_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _treats a typographic apostrophe as an apostrophe_
- **`bearing_drops_short_tokens`** — The three-character floor `contentTokens` uses for routing comes back to the receipt, and `no` stops existing.
  - claimed catcher(s): _keeps two-letter words, because "no" inverts a sentence_
  - failing tests (54):
    - _checks a sentence written in a language with no ASCII in it at all_
    - _counts a repeated word twice, so double negation is not one negation_
    - _covers every problem kind it declares_
    - _does not accept a quote whose code delimiters the model dropped_
    - _does not accept a quote whose emphasis the model dropped_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not certify an inline code sample as its author"s assertion_
    - _does not forgive the backticks, because a code span is mention and not use_
    - _does not tell a room the quote lacks words the quote has_
    - _does not treat a contraction as its expansion_
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _is borne by itself, for every text in the corpus that has content_
    - _is borne exactly when the two normalized texts are the same string_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _keeps two-letter words, because "no" inverts a sentence_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles a text built from any code point in plane 0, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 1, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 10, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 11, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 12, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 13, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 14, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 15, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 16, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 2, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 3, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 4, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 5, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 6, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 7, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 8, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 9, not only the ones somebody listed_
    - _reassembles the normalized text exactly, for every text_
    - _refers it rather than rejecting it_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a difference of a full stop, like every other difference_
    - _refuses a negation written in another script or another width_
    - _refuses a question minted as an assertion_
    - _refuses a statement that inserts a word the quote does not contain_
    - _refuses a statement that runs past the end of its quote_
    - _refuses a struck-through sentence bearing its own assertion_
    - _refuses a symbol that carries the negation_
    - _refuses an article substitution, which laundered an indefinite reference_
    - _refuses an input too large to align rather than approximating it_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _swept every code point across the shards, with nothing between them_
    - _treats a typographic apostrophe as an apostrophe_
- **`receipt_policy_droppable_restored`** — Puts `droppableTokens` back, with the entry r6 removed. Every entry it ever held was broken by a reviewer on the same shape — a justification about *context* enforced by a set-membership test over a *character*. `.` was there because "a full stop terminates a sentence and carries no other meaning", and grok's blind pass produced ``Load `.env` before the deploy tonight.`` bearing ``Load `env` …``, `` `..` `` bearing `` `` ``, and "We ship Monday. Then we celebrate." bearing "We ship Monday Then we celebrate." — all auto-accepted and all folded through `appendEvent`.
  - claimed catcher(s): _lets nothing at all differ between a quote and its statement_; _refuses a difference of a full stop, like every other difference_; _refuses the code literal a dropped full stop used to rewrite_; _is borne exactly when the two normalized texts are the same string_
  - failing tests (5):
    - _is borne exactly when the two normalized texts are the same string_
    - _lets nothing at all differ between a quote and its statement_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a difference of a full stop, like every other difference_
    - _refuses the code literal a dropped full stop used to rewrite_
- **`receipt_policy_align_cap_raised`** — The bound on the O(n*m) alignment is lifted, so the cost of a receipt check is chosen by whoever writes the quote.
  - claimed catcher(s): _refuses an input too large to align rather than approximating it_; _is exactly this table_
  - failing tests (4):
    - _is exactly this table_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _refers it rather than rejecting it_
    - _refuses an input too large to align rather than approximating it_
- **`statement_uncheckable_skipped`** — Skips the `undecidable` branch entirely, so the bearing check's three refusals-to-run stop being reported as themselves. Since r6 the branch below it — `!borne`, with a named case for every way the diff can end — catches the empty quote and the empty statement and refuses them anyway, which is the second line working. What the mutation still destroys is the split the branch exists for: `too_long` loses its `refer` and is discarded as a malformed reading.
  - claimed catcher(s): _refers it rather than rejecting it_
  - failing tests (1):
    - _refers it rather than rejecting it_
- **`refer_severity_ignored_reducer`** — The reducer stops refusing an acceptance whose receipt the check declines to rule on, so `refer` fails open at the trust boundary.
  - claimed catcher(s): _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
  - failing tests (7):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _refuses the padded window at the reducer, where it is a boundary_
    - _reports the receipt fault rather than the confidence, when both hold_
- **`refer_severity_ignored_engine`** — The same fail-open at the acceptance engine: an unjudgeable receipt falls through to auto-accept.
  - claimed catcher(s): _refuses the exact pair from the gauntlet at the acceptance engine_; _refers a paraphrase of the quote to a person instead of accepting it_
  - failing tests (20):
    - _does not auto-accept it at the engine_
    - _does not auto-accept it at the engine either_
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _keeps the disposition — an unread body is not a clean one_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _reaches every rule name the type declares_
    - _refers a paraphrase of the quote to a person instead of accepting it_
    - _refers an inverted receipt that sits below theta_min_
    - _refers it rather than rejecting it_
    - _refers when the second telling adds words to the first_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a quote mined out of a first-person refusal_
    - _refuses a quote mined out of a hedge_
    - _refuses a quote mined out of a report of what nobody thinks_
    - _refuses a quote mined out of an explicit denial_
    - _refuses every one of the four at the engine_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the padded multi-citation window at both enforcement points_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
- **`refer_collapsed_into_reject`** — The two negative answers are collapsed: a reading the check cannot rule on is discarded instead of referred, which hides the discrepancy a person needs to see.
  - claimed catcher(s): _leaves the reading staged rather than deleting it, so a person sees the discrepancy_; _refers a paraphrase of the quote to a person instead of accepting it_
  - failing tests (12):
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _reaches every rule name the type declares_
    - _refers a paraphrase of the quote to a person instead of accepting it_
    - _refers an inverted receipt that sits below theta_min_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _reports the receipt fault rather than the confidence, when both hold_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
- **`attention_certifiable_silence_dropped`** — The panel stops recording why it went quiet about a proposal whose receipt cannot be certified, so the silence has no receipt.
  - claimed catcher(s): _silences the confirm request and records why, rather than asking Bob to confirm it_
  - failing tests (1):
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
- **`blank_is_trim`** — r3 gauntlet polish: emptiness goes back to being whitespace-only, so a zero-width space counts as content and every required-input refusal becomes incidental.
  - claimed catcher(s): _reads a zero-width character as no content at all_; _treats a quote of invisible characters as no quote at all_
  - failing tests (7):
    - _names that gate for every model-minted type, not only the two that name a person_
    - _reads a zero-width character as no content at all_
    - _refuses a window of messages that exist and say nothing, at the engine_
    - _refuses a window of messages that exist and say nothing, at the reducer_
    - _says it read nothing, rather than reporting a clean scan of nothing_
    - _still calls punctuation and invisible characters absence_
    - _treats a quote of invisible characters as no quote at all_
- **`normalize_keeps_invisibles`** — The other half: an invisible character spliced into a quote makes it a different string from the message it came out of, so a correct receipt fails for the wrong reason.
  - claimed catcher(s): _matches a quote whose words a zero-width character was spliced into_
  - failing tests (9):
    - _deletes exactly this set of code points, and fuses two words with none of them_
    - _does not delete a bidi override, which decides what the reader sees_
    - _is a statement the receipt genuinely certifies_
    - _lets nothing at all differ between a quote and its statement_
    - _matches a quote whose words a zero-width character was spliced into_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _sees the same words through every difference the receipt forgives_
- **`ordered_tokens_ascii_only`** — r4's own blind review: the tokenizer split on /[^a-z0-9']+/, which is a denylist of the characters that may carry meaning. Cyrillic and fullwidth negations were deleted as separators and bore their own affirmative; a Russian sentence tokenized to nothing and was refused as empty. Mutates the whole token rule rather than only the word class, because since r6 an ASCII-only word class alone leaves every other script matching the code-point alternative — the same evasion has to be applied to the rule that replaced it.
  - claimed catcher(s): _refuses a negation written in another script or another width_; _checks a sentence written in a language with no ASCII in it at all_
  - failing tests (51):
    - _checks a sentence written in a language with no ASCII in it at all_
    - _counts a repeated word twice, so double negation is not one negation_
    - _covers every problem kind it declares_
    - _does not accept a quote whose code delimiters the model dropped_
    - _does not accept a quote whose emphasis the model dropped_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not certify an inline code sample as its author"s assertion_
    - _does not forgive the backticks, because a code span is mention and not use_
    - _does not tell a room the quote lacks words the quote has_
    - _does not treat a contraction as its expansion_
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _is borne exactly when the two normalized texts are the same string_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _keeps two-letter words, because "no" inverts a sentence_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles a text built from any code point in plane 0, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 1, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 10, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 11, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 12, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 13, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 14, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 15, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 16, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 2, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 3, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 4, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 5, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 6, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 7, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 8, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 9, not only the ones somebody listed_
    - _reassembles the normalized text exactly, for every text_
    - _refers it rather than rejecting it_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a difference of a full stop, like every other difference_
    - _refuses a negation written in another script or another width_
    - _refuses a question minted as an assertion_
    - _refuses a struck-through sentence bearing its own assertion_
    - _refuses a symbol that carries the negation_
    - _refuses an article substitution, which laundered an indefinite reference_
    - _refuses an input too large to align rather than approximating it_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _swept every code point across the shards, with nothing between them_
    - _treats a typographic apostrophe as an apostrophe_
- **`ordered_tokens_drops_punctuation`** — The other half of r4's denylist: a visible mark stops being a token, so `Bob will deploy Friday?` bears the assertion and a leading ❌ or 🚫 vanishes. Since r6 the same edit also deletes every space and every standalone apostrophe, so the token stream stops reassembling the text it came from — which is the property the whole receipt rests on.
  - claimed catcher(s): _refuses a question minted as an assertion_; _refuses a symbol that carries the negation_; _lets nothing at all differ between a quote and its statement_; _reassembles the normalized text exactly, for every text_
  - failing tests (48):
    - _checks a sentence written in a language with no ASCII in it at all_
    - _counts a repeated word twice, so double negation is not one negation_
    - _covers every problem kind it declares_
    - _does not accept a quote whose code delimiters the model dropped_
    - _does not accept a quote whose emphasis the model dropped_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not certify an inline code sample as its author"s assertion_
    - _does not forgive the backticks, because a code span is mention and not use_
    - _does not treat a contraction as its expansion_
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _is borne exactly when the two normalized texts are the same string_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _keeps two-letter words, because "no" inverts a sentence_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles a text built from any code point in plane 0, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 1, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 10, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 11, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 12, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 13, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 14, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 15, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 16, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 2, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 3, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 4, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 5, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 6, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 7, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 8, not only the ones somebody listed_
    - _reassembles a text built from any code point in plane 9, not only the ones somebody listed_
    - _reassembles the normalized text exactly, for every text_
    - _refers it rather than rejecting it_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a difference of a full stop, like every other difference_
    - _refuses a question minted as an assertion_
    - _refuses a struck-through sentence bearing its own assertion_
    - _refuses a symbol that carries the negation_
    - _refuses an input too large to align rather than approximating it_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _swept every code point across the shards, with nothing between them_
    - _treats a typographic apostrophe as an apostrophe_
- **`normalize_folds_emphasis`** — Re-admits asterisk emphasis to the difference allowlist. It was there for three drafts and broke twice: `a*b*c` folds to `abc`, and `2*3*4` folds to `234` — which changes how many, in the one direction the entry's own justification promised it could not.
  - claimed catcher(s): _does not fold an asterisk that is doing arithmetic or globbing_
  - failing tests (3):
    - _does not accept a quote whose emphasis the model dropped_
    - _does not fold an asterisk that is doing arithmetic or globbing_
    - _refuses the quote a model produced by dropping the emphasis_
- **`quote_anchoring_disabled`** — r4's own blind review, and the strongest finding of the round: the quote goes back to being any substring of the cited message, so a model that sets quote === statement to an affirmative span leaves `it is not true that` outside the scissors and auto-accepts the inversion.
  - claimed catcher(s): _refuses a quote mined out of an explicit denial_; _refuses a quote mined out of a report of what nobody thinks_; _refuses a quote mined out of a hedge_; _refuses a quote mined out of a first-person refusal_
  - failing tests (6):
    - _covers every problem kind it declares_
    - _refuses a quote mined out of a first-person refusal_
    - _refuses a quote mined out of a hedge_
    - _refuses a quote mined out of a report of what nobody thinks_
    - _refuses a quote mined out of an explicit denial_
    - _still calls a real fragment a fragment, under the cap_
- **`quote_anchoring_allows_a_prefix`** — The anchor accepts a quote that merely *starts* a sentence rather than covering whole ones, which readmits every trailing qualifier — `we ship Friday` mined out of `we ship Friday unless CI is red`.
  - claimed catcher(s): _refuses a quote that stops before the qualifier in its own sentence_
  - failing tests (1):
    - _refuses a quote that stops before the qualifier in its own sentence_
- **`sentence_split_ignores_terminators`** — Sentences stop being split on `.`/`?`/`!`, so a multi-sentence message is one sentence and every honest single-sentence quote out of it becomes a fragment — the anchor inverted into an over-refusal nobody would notice until the product refused everything.
  - claimed catcher(s): _accepts a run of several whole sentences, and refuses half of one_
  - failing tests (6):
    - _accepts a run of several whole sentences, and refuses half of one_
    - _covers every problem kind it declares_
    - _does not certify a quote whose message carries a trailing inverter_
    - _refers a leading sentence too — the scissors cut both ways_
    - _refers every other way a neighbouring sentence changes the force_
    - _says the check declined rather than calling a whole body a fragment_
- **`typographic_apostrophe_unfolded`** — `won’t` stops being `won't`, so a faithful quotation is refused for its typography — an over-refusal, and the kind that gets 'fixed' later by weakening the check that matters.
  - claimed catcher(s): _treats a typographic apostrophe as an apostrophe_
  - failing tests (3):
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _lets nothing at all differ between a quote and its statement_
    - _treats a typographic apostrophe as an apostrophe_
- **`quote_context_unchecked`** — Drops the rule that a certifiable quote is the whole of what its author wrote. This is r4's documented residue: "We will deploy production Friday. Not." quoted as its first sentence is verbatim, whole-sentence, correctly attributed, and bears its own statement.
  - claimed catcher(s): _does not certify a quote whose message carries a trailing inverter_; _refers every other way a neighbouring sentence changes the force_
  - failing tests (11):
    - _covers every problem kind it declares_
    - _does not auto-accept it at the engine either_
    - _does not certify a quote whose message carries a trailing inverter_
    - _does not police attribution on types that name nobody — but still requires their receipt_
    - _finds a quote reproduced exactly, and reports only the scissors_
    - _reads a line break the way every renderer does, not the way a newline does_
    - _refers a leading sentence too — the scissors cut both ways_
    - _refers a quote whose author put the inverter in their own blockquote_
    - _refers every other way a neighbouring sentence changes the force_
    - _refers the same claim when its author wrote more around the sentence_
    - _requires a commitment’s owner to have authored a cited message_
- **`later_correction_unseen`** — Stops acceptance looking past the cited messages, so a window holding "We will deploy production Friday." followed by "Correction: we will not deploy production Friday." certifies the first on a proposal that cites only it.
  - claimed catcher(s): _refuses to certify a sentence a later message in the window corrects_; _refuses to certify when a later message takes something back in other words_
  - failing tests (14):
    - _cannot be skipped by citing the correction itself_
    - _cannot be skipped by padding the citation list_
    - _covers every problem kind it declares_
    - _does not auto-accept it at the engine_
    - _does not let a padded citation list flip attribution_
    - _reads the same disposition end to end for both citations_
    - _refers when the second telling adds words to the first_
    - _refuses a window that stops where the citations stop_
    - _refuses the padded multi-citation window at both enforcement points_
    - _refuses the padded window at the reducer, where it is a boundary_
    - _refuses to certify a sentence a later message in the window corrects_
    - _refuses to certify when a later message drops a word from the sentence_
    - _refuses to certify when a later message takes something back in other words_
    - _refuses when the window is longer than the scan will read_
- **`speech_act_unchecked`** — Lets a question be minted as a claim. The payload schemas require only a nonempty string and the receipt proves string equality, so "Would we deploy production Friday?" as a Claim has a perfect receipt and turns somebody's question into their position.
  - claimed catcher(s): _does not mint a question as a claim_
  - failing tests (6):
    - _covers every problem kind it declares_
    - _does not mint a question as a claim_
    - _reads a mark a canonical decomposition would destroy_
    - _reads a mark that opens the sentence instead of closing it_
    - _refuses an assertion on an unverifiable mark without certifying a question_
    - _refuses an ornament question mark minted as a claim — r5’s defect, reopened_
- **`refer_below_theta_again`** — Puts the referral check back under the theta_min discard, where r4 had it. An inverted receipt at confidence 0.4999 is then discarded as weak, destroying the exact discrepancy the receipt exists to show.
  - claimed catcher(s): _refers an inverted receipt that sits below theta_min_
  - failing tests (1):
    - _refers an inverted receipt that sits below theta_min_
- **`commitment_model_mintable`** — Lets a machine mint a commitment again. #44's fact-check drove a model actor through the whole stack with {statement: 'Bob will deploy production Friday', owner: 'user_bob'} and it landed with zero issues.
  - claimed catcher(s): _refuses a model accepting a commitment before the receipt is even read_
  - failing tests (20):
    - _gates exactly the types a machine may not perform, and floors the ones it cannot certify_
    - _model_other accepts commitment via human @above floor_
    - _model_other accepts commitment via human @below floor_
    - _model_other accepts commitment via model_a @above floor_
    - _model_other accepts commitment via model_a @below floor_
    - _model_other accepts commitment via none_
    - _model_proposer accepts commitment via human @above floor_
    - _model_proposer accepts commitment via human @below floor_
    - _model_proposer accepts commitment via model_a @above floor_
    - _model_proposer accepts commitment via model_a @below floor_
    - _model_proposer accepts commitment via none_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model accepting a commitment before the receipt is even read_
    - _refuses a model accepting a commitment somebody else’s sentence named_
    - _refuses it even when the owner wrote the sentence himself_
    - _system accepts commitment via human @above floor_
    - _system accepts commitment via human @below floor_
    - _system accepts commitment via model_a @above floor_
    - _system accepts commitment via model_a @below floor_
    - _system accepts commitment via none_
- **`objective_model_mintable`** — Lets a machine mint an objective — the heading everything else in the room is filed under, and one that already needs a person to retire.
  - claimed catcher(s): _refuses a model accepting an objective — the heading everything is filed under_
  - failing tests (18):
    - _gates exactly the types a machine may not perform, and floors the ones it cannot certify_
    - _model_other accepts objective via human @above floor_
    - _model_other accepts objective via human @below floor_
    - _model_other accepts objective via model_a @above floor_
    - _model_other accepts objective via model_a @below floor_
    - _model_other accepts objective via none_
    - _model_proposer accepts objective via human @above floor_
    - _model_proposer accepts objective via human @below floor_
    - _model_proposer accepts objective via model_a @above floor_
    - _model_proposer accepts objective via model_a @below floor_
    - _model_proposer accepts objective via none_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a model accepting an objective — the heading everything is filed under_
    - _system accepts objective via human @above floor_
    - _system accepts objective via human @below floor_
    - _system accepts objective via model_a @above floor_
    - _system accepts objective via model_a @below floor_
    - _system accepts objective via none_
- **`normalize_deletes_backticks`** — Deletes the backticks a code span is made of, as r4 did. A backticked span is mention rather than use, and folding the delimiters away turns a string its author displayed into a sentence its author asserted.
  - claimed catcher(s): _does not accept a quote whose code delimiters the model dropped_
  - failing tests (11):
    - _deletes exactly this set of code points, and fuses two words with none of them_
    - _does not accept a quote whose code delimiters the model dropped_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not certify an inline code sample as its author"s assertion_
    - _does not forgive the backticks, because a code span is mention and not use_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
- **`normalize_drops_link_destination`** — Collapses a markdown link to its text, as r4 did. `Use [https://safe.example/app](https://evil.example/app) today.` then certifies a statement naming the safe URL against a record whose actionable link points somewhere else. r7 unified the two folds onto one `unfoldLink`, so this single mutation now reaches both call sites — which is the point of there being one.
  - claimed catcher(s): _keeps a markdown link"s destination as content_; _keeps a markdown link"s destination, exactly as the receipt fold does_
  - failing tests (4):
    - _does not let a markdown link"s destination vanish_
    - _is the same policy in both folds, which is what "one answer" means_
    - _keeps a markdown link"s destination as content_
    - _keeps a markdown link"s destination, exactly as the receipt fold does_
- **`normalize_refolds_nfkc`** — Re-applies NFKC compatibility folding on the receipt path, so two distinct hostnames and two distinct identifiers compare equal.
  - claimed catcher(s): _does not fold a fullwidth or ligature spelling onto its ASCII form_
  - failing tests (6):
    - _does not fold a fullwidth or ligature spelling onto its ASCII form_
    - _does not fold distinct identifiers onto each other_
    - _is the only fold allowed to be this lossy, and it never sees a receipt_
    - _keeps `10² ms` and `102 ms` apart_
    - _matches exactly the marks it enumerates, and no others_
    - _reads a mark a canonical decomposition would destroy_
- **`content_excludes_pictographs`** — Takes emoji and flags back out of what counts as content, so a message that is a row of 🚫 or a single 🇺🇸 reduces to "nothing" and its quote is refused as blank — a failure wearing another failure's clothes, and a package disagreeing with its own tokenizer.
  - claimed catcher(s): _counts an emoji-only message as having something in it_; _counts a flag as content, which is not a pictograph_
  - failing tests (3):
    - _counts a flag as content, which is not a pictograph_
    - _counts an emoji-only message as having something in it_
    - _does not disguise an emoji-only quote as a blank one_
- **`receipt_after_confidence_floor`** — Puts the reducer's receipt check back under the confidence floor, so an acceptance whose quote says the opposite of its statement is refused as 'confidence too low' and the serious finding never reaches the room's log.
  - claimed catcher(s): _reports the receipt fault rather than the confidence, when both hold_
  - failing tests (1):
    - _reports the receipt fault rather than the confidence, when both hold_
- **`later_correction_cap_fails_open`** — Makes the later-correction scan return 'nothing found' when the window is longer than it will read, instead of refusing. This round's own first draft did exactly that and defended it in a comment — a limit written down with an auto-accept under it, which is the shape the whole round exists to delete.
  - claimed catcher(s): _refuses when the window is longer than the scan will read_
  - failing tests (1):
    - _refuses when the window is longer than the scan will read_
- **`question_mark_folded_first`** — Normalizes before the question-mark test, which is what the second repair did. NFKC is redundant against the completed inventory and actively destroys one mark: U+037E GREEK QUESTION MARK canonically decomposes to an ASCII semicolon, so the folded form reads as punctuation.
  - claimed catcher(s): _reads a mark a canonical decomposition would destroy_
  - failing tests (2):
    - _matches exactly the marks it enumerates, and no others_
    - _reads a mark a canonical decomposition would destroy_
- **`code_span_whitespace_collapsed`** — Carries the whitespace-collapse rule into a code literal, so `` `a  b` `` and `` `a b` `` compare equal. Two spaces in prose are a line wrap; two spaces in a password are a different password.
  - claimed catcher(s): _refuses to mint a code literal the author did not write, at every layer_
  - failing tests (5):
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
- **`dedup_destroys_a_referral`** — Puts the duplicate discard back above the referral, where it deletes a detected receipt discrepancy the room has never been shown — the same destruction the theta_min ordering was fixed for, through a different door.
  - claimed catcher(s): _does not let deduplication destroy a discrepancy nobody has seen_
  - failing tests (1):
    - _does not let deduplication destroy a discrepancy nobody has seen_
- **`bidi_controls_deleted`** — Folds every `\p{Cf}` away again — the bidi overrides, the soft hyphen and the zero-width joiner with the genuinely invisible characters. `Bob will \u202Eton\u202C deploy production Friday.` renders as "Bob will not deploy production Friday."; `re\u00ADsign` renders as "re-sign"; and `\u{1F469}\u200D\u{1F4BB}` is one glyph where `\u{1F469}\u{1F4BB}` is two.
  - claimed catcher(s): _does not delete a bidi override, which decides what the reader sees_; _deletes exactly this set of code points, and fuses two words with none of them_
  - failing tests (5):
    - _deletes exactly this set of code points, and fuses two words with none of them_
    - _does not delete a bidi override, which decides what the reader sees_
    - _does not delete a character that renders as something_
    - _names the difference in a form a reader can act on_
    - _never refuses a message quoted as itself_
- **`later_scan_starts_after_last_citation`** — Starts the later-correction scan after the *last* cited message instead of at or before the sentence, so padding the citation list with an unrelated later message hides a correction behind the scan's start. The same attack r1's gauntlet found against the attribution check. **Restated in r8**: it used to mutate `firstCited`, and r8's text-derived floor made that form stop discriminating — `earliestBearing` finds the sentence whatever the citations say, so the r6 mutation was caught by nothing it claimed. The rule is unchanged; the line that carries it moved, and a mutant left pointing at the old line is a row that has quietly stopped measuring anything.
  - claimed catcher(s): _cannot be skipped by padding the citation list_
  - failing tests (6):
    - _cannot be skipped by citing the correction itself_
    - _cannot be skipped by padding the citation list_
    - _finds the correction whichever copy of the sentence is cited_
    - _lets a genuinely later window through, so this is not "refuse every window"_
    - _reads the same disposition end to end for both citations_
    - _still reads a correction sitting between two citations_
- **`question_mark_set_narrowed`** — Drops the inverted question mark, which a Spanish interrogative opens with and may carry no other mark at all — `¿Bob will deploy production Friday` minted as a claim.
  - claimed catcher(s): _reads a mark that opens the sentence instead of closing it_
  - failing tests (1):
    - _reads a mark that opens the sentence instead of closing it_
- **`coverage_reads_the_stripped_text`** — Points the coverage check at `stripReplyBlockquotes(body)` instead of the body, which is r5's first draft. `stripReplyBlockquotes` deletes every line beginning with `>` whether or not anybody else wrote it, so an author who writes "We will deploy production Friday." and then "> Not." has their own second line deleted and the first certifies.
  - claimed catcher(s): _refers a quote whose author put the inverter in their own blockquote_
  - failing tests (1):
    - _refers a quote whose author put the inverter in their own blockquote_
- **`later_revision_addition_only`** — Asks only whether a later message *adds* to the statement, so a correction that says the same thing with a word **removed** — the r3 inversion, mirrored — reads as unrelated and the earlier reading auto-accepts.
  - claimed catcher(s): _refuses to certify when a later message drops a word from the sentence_
  - failing tests (2):
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _refuses to certify when a later message drops a word from the sentence_
- **`later_scan_skips_cited_messages`** — Filters the cited messages out of the later-correction scan, so putting the correction in a message the proposal *cites* steps over the check that exists to find corrections. A citation is chosen by the proposal, so anything it can exclude is a boundary the proposal controls.
  - claimed catcher(s): _cannot be skipped by citing the correction itself_
  - failing tests (2):
    - _cannot be skipped by citing the correction itself_
    - _lets a genuinely later window through, so this is not "refuse every window"_
- **`whitespace_controls_deleted`** — Puts the control-character clause back: `(?!\s)\p{Cc}` alongside the enumeration. Its docblock argued the clause could not fuse two words because "tab, newline, vertical tab, form feed, carriage return and NEL are `\s`" — and JavaScript's `\s` does not contain U+0085 NEL, U+007F, or U+0080–U+009F. The clause therefore deleted them, and `Bob will deploy<NEL>production Friday.` normalized to `deployproduction`, which a quote of the fused text matched. NEL is a Unicode mandatory line break, so deleting it changes what a reader sees. The mutant's own `why` said the opposite until r6.
  - claimed catcher(s): _deletes exactly this set of code points, and fuses two words with none of them_
  - failing tests (2):
    - _deletes exactly this set of code points, and fuses two words with none of them_
    - _names the difference in a form a reader can act on_
- **`whitespace_run_uses_js_s`** — Reads the whitespace run as JavaScript's `\s` instead of Unicode's `\p{White_Space}`. The two differ on U+0085 NEL — a mandatory line break that `\s` does not contain — so a NEL between two words stops collapsing to a space, and the fold that was supposed to own it does not.
  - claimed catcher(s): _deletes exactly this set of code points, and fuses two words with none of them_
  - failing tests (1):
    - _deletes exactly this set of code points, and fuses two words with none of them_
- **`ordered_tokens_drops_spaces`** — r6's finding, exactly: the tokenizer discards whitespace, so the token comparison proves word equality and the receipt claims text equality. ``Run `rm -rf / tmp/cache` …`` then bears ``Run `rm -rf /tmp/cache` …`` and a doubled space in a password is invisible — the case `normalizeForReceipt` passes code spans through byte for byte to prevent.
  - claimed catcher(s): _reassembles the normalized text exactly, for every text_; _refuses every one of the four at the engine_; _refuses every one of the four at the reducer, where it is a boundary_; _refuses to mint a code literal the author did not write, at every layer_
  - failing tests (20):
    - _checks a sentence written in a language with no ASCII in it at all_
    - _counts a repeated word twice, so double negation is not one negation_
    - _covers every problem kind it declares_
    - _does not treat a contraction as its expansion_
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _is borne exactly when the two normalized texts are the same string_
    - _is past the cap, so this test is about the cap and not about a long sentence_
    - _keeps two-letter words, because "no" inverts a sentence_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles a text built from any code point in plane 0, not only the ones somebody listed_
    - _reassembles the normalized text exactly, for every text_
    - _refers it rather than rejecting it_
    - _refuses an input too large to align rather than approximating it_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _swept every code point across the shards, with nothing between them_
    - _treats a typographic apostrophe as an apostrophe_
- **`ordered_tokens_drops_apostrophes`** — The other half of r6's finding: a standalone ASCII apostrophe stops being a token, so `'We will deploy production Friday.'` — a displayed sentence — bears the bare assertion. Backticks and double quotes are content for exactly this reason; the apostrophe was the one spelling of mention-versus-use nothing could see.
  - claimed catcher(s): _reads an edge apostrophe as a mark of its own, not as part of the word_; _refuses every one of the four at the engine_; _refuses every one of the four at the reducer, where it is a boundary_
  - failing tests (6):
    - _is borne exactly when the two normalized texts are the same string_
    - _lets nothing at all differ between a quote and its statement_
    - _reads an edge apostrophe as a mark of its own, not as part of the word_
    - _reassembles the normalized text exactly, for every text_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
- **`routing_tokens_keep_spaces`** — Gives the detector the receipt's exhaustive stream. The routing fold has already collapsed every whitespace run, so a space token carries no information there and only confuses the resynchronising diff: an inserted word arrives with an inserted space, and the correction scan reads a plain contradiction as an unrelated sentence.
  - claimed catcher(s): _drops whitespace from the detector"s tokens and only from the detector"s_
  - failing tests (2):
    - _drops whitespace from the detector"s tokens and only from the detector"s_
    - _refers it rather than rejecting it_
- **`align_borne_read_off_the_diff`** — Computes `borne` from the resynchronising diff again instead of from `sameRun`. The diff is a judgement about which tokens correspond and it does not name a whitespace difference, so a re-spaced code literal comes back borne — the r6 defect, reintroduced one layer in from the tokenizer.
  - claimed catcher(s): _refuses to mint a code literal the author did not write, at every layer_; _lets nothing at all differ between a quote and its statement_; _refuses every one of the four at the engine_
  - failing tests (7):
    - _covers every problem kind it declares_
    - _is borne exactly when the two normalized texts are the same string_
    - _lets nothing at all differ between a quote and its statement_
    - _names the difference in a form a reader can act on_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
- **`respacing_not_reported`** — Drops the whitespace branch, so a statement holding the same marks in a different spacing falls through to the catch-all. The catch-all still refuses — that is what it is for — but the receipt stops saying what it found, and a refusal nobody can act on is the failure this file spends its length on. The branch says the fact and not the cause: three drafts of this round tried to enumerate the ways two texts can differ in spacing alone and a brute force broke all three.
  - claimed catcher(s): _refuses to mint a code literal the author did not write, at every layer_; _names the difference in a form a reader can act on_; _covers every problem kind it declares_
  - failing tests (3):
    - _covers every problem kind it declares_
    - _names the difference in a form a reader can act on_
    - _refuses to mint a code literal the author did not write, at every layer_
- **`uncheckable_too_long_rejected`** — Collapses the two reasons the bearing check declines back into one severity. `too_long` then rejects, `acceptance.ts` turns a reject into a discard, and a 421-token design comment quoted whole and correctly attributed is destroyed — while the room is told its citation failed, which is not what happened. `refer` is defined as never auto-accepted and never discarded.
  - claimed catcher(s): _refers it rather than rejecting it_
  - failing tests (1):
    - _refers it rather than rejecting it_
- **`later_revision_window_gate_removed`** — Removes the check that the correction scan actually read something the proposal did not choose. A caller supplying 'the messages this receipt cites' — the natural reading of the field — then turns the scan off in silence: whole room refuses, cited-only auto-accepts. Same fail-open as messages: [] and [{ body: '' }], one level out. r6's first repair asked whether the *window* held an uncited message, and grok's blind pass padded it with an *earlier* one — which cannot correct a later message — so the question is what the scan read. r7 moved the gate's existence test from the earliest citation to the newest; the mutant that restores r6's version of it is `later_revision_gate_reads_the_earliest_citation`.
  - claimed catcher(s): _refuses a window that stops where the citations stop_; _says how far it read, so "found nothing" is not "read nothing"_; _reads the newest citation, not the earliest, so a second citation buys nothing_
  - failing tests (8):
    - _cannot be helped by citing something later either_
    - _does not let a padded citation list flip attribution_
    - _is a tagged union, so the three answers cannot be told apart by truthiness_
    - _reads the newest citation, not the earliest, so a second citation buys nothing_
    - _refuses a window that stops where the citations stop_
    - _refuses the padded multi-citation window at both enforcement points_
    - _refuses the padded window at the reducer, where it is a boundary_
    - _says how far it read, so "found nothing" is not "read nothing"_
- **`later_revision_gate_reads_the_earliest_citation`** — r7's finding, and r6's shipped code. The window gate's existence test ran from `firstCited + 1` — the index the *scan* starts at — so it passed as soon as some uncited message sat after the *earliest* citation. `provenance` is model-supplied, so citing one extra *earlier* message turned the gate off while every message the scan read sat at or before the sentence: `[cited, uncited, cited]` went from `pending` to `auto_accept`, at the engine and at the reducer both. A rule over a value the proposer controls measures the proposer's cooperation.
  - claimed catcher(s): _reads the newest citation, not the earliest, so a second citation buys nothing_; _refuses the padded multi-citation window at both enforcement points_; _refuses the padded window at the reducer, where it is a boundary_
  - failing tests (4):
    - _does not let a padded citation list flip attribution_
    - _reads the newest citation, not the earliest, so a second citation buys nothing_
    - _refuses the padded multi-citation window at both enforcement points_
    - _refuses the padded window at the reducer, where it is a boundary_
- **`token_rendered_raw`** — Interpolates an unmatched token straight into the refusal. `orderedTokens` is exhaustive since r6, so a token can be a control character with no glyph, and the receipt then reports `the quote says ""` — a check that ran, found something, and said nothing a reader can use.
  - claimed catcher(s): _names the difference in a form a reader can act on_
  - failing tests (1):
    - _names the difference in a form a reader can act on_
- **`bearing_result_ignored`** — Drops the whole `!borne` branch, so a bearing check that ran and refused reports nothing at all — the fail-open shape this ticket has found in six different places, at the one comparison the acceptance path makes between the quote and the statement. It is the mutation `statement_uncheckable_skipped` used to stand in for before r6 gave the branch a named case for every way the diff can end.
  - claimed catcher(s): _refuses the exact pair from the gauntlet at the acceptance engine_; _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_; _covers every problem kind it declares_; _refuses every one of the four at the engine_; _refuses to mint a code literal the author did not write, at every layer_
  - failing tests (37):
    - _covers every problem kind it declares_
    - _covers the same space the unsharded loop did, shard by shard_
    - _does not certify a fenced code block as its author"s assertion_
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _does not tell a room the quote lacks words the quote has_
    - _keeps the verdict, which was never the defect_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _model_proposer accepts a claim with a uncertifiable receipt_
    - _names the difference in a form a reader can act on_
    - _never refuses a bearing check without naming the difference (quote insert "  ")_
    - _never refuses a bearing check without naming the difference (quote insert " ")_
    - _never refuses a bearing check without naming the difference (quote insert "'")_
    - _never refuses a bearing check without naming the difference (quote insert ",")_
    - _never refuses a bearing check without naming the difference (quote insert ".")_
    - _never refuses a bearing check without naming the difference (quote insert "?")_
    - _never refuses a bearing check without naming the difference (quote insert "\n")_
    - _never refuses a bearing check without naming the difference (quote insert "\t")_
    - _never refuses a bearing check without naming the difference (quote insert "\u0001")_
    - _never refuses a bearing check without naming the difference (quote insert "`")_
    - _never refuses a bearing check without naming the difference (quote insert "a")_
    - _never refuses a bearing check without naming the difference (quote insert "​")_
    - _never refuses a bearing check without naming the difference (quote insert "‮")_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refers a paraphrase of the quote to a person instead of accepting it_
    - _refers an inverted receipt that sits below theta_min_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses a verbatim, correctly attributed quote that carries a different sentence_
    - _refuses every one of the four at the engine_
    - _refuses every one of the four at the reducer, where it is a boundary_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the exact pair from the gauntlet at the reducer, where it is a boundary_
    - _refuses the same thing at the reducer, where it is a trust boundary_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _rejects a quote elided with an ellipsis that is not verbatim anywhere_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
    - _still names a word that really is absent, so this is not "never say absent"_
- **`line_break_is_newline_only`** — Reads a line break as \n alone again, which is the same defect `\s` had for whitespace one function over: U+0085 NEL, U+2028 and U+2029 end a line everywhere a line is rendered and `\n` sees none of them. A body whose breaks are NEL becomes one line, so a reply-blockquote on its first line swallows the author's own words and a multi-sentence body reads as a single sentence. Found by this round running its own `\p{White_Space}` fix back over the rest of the package.
  - claimed catcher(s): _reads a line break the way every renderer does, not the way a newline does_
  - failing tests (1):
    - _reads a line break the way every renderer does, not the way a newline does_
- **`fragment_scan_cap_reports_a_fragment`** — r7. Removes the cap branch, so a body with more sentences than the scan will read falls through to `quoteSpansWholeSentences` returning false and the room is told the quote was 'cut out of the middle of a sentence' — about a quote that may be the entire body. Right disposition, false sentence; the same class as the transposition mislabel one function over.
  - claimed catcher(s): _says the check declined rather than calling a whole body a fragment_
  - failing tests (2):
    - _covers every problem kind it declares_
    - _says the check declined rather than calling a whole body a fragment_
- **`link_title_discarded`** — r7. Drops the markdown link title again, so `[the runbook](https://x.example "Do NOT run step 4")` folds to text the author's warning is missing from, and a statement omitting the warning auto-accepts. The same argument r4 used to make the destination content — the title is author-written text a reader sees.
  - claimed catcher(s): _keeps a link title, which is text the author wrote_
  - failing tests (2):
    - _is the same policy in both folds, which is what "one answer" means_
    - _keeps a link title, which is text the author wrote_
- **`image_marker_discarded`** — r7. Drops the `!` image marker, so `![alt](url)` and `[alt](url)` — an embed and a link — normalize to the same text and a statement about one is certifiable against a record carrying the other.
  - claimed catcher(s): _keeps the image marker, so an embed is not a link_
  - failing tests (1):
    - _keeps the image marker, so an embed is not a link_
- **`later_revision_no_citation_reads_clean`** — r7, found by the round's foreign-lineage pass. `laterRevision` returns the value meaning 'the scan ran to the end and found nothing' when no cited id is in the window — so 'checked, clean' and 'never looked' become one value again, which is the exact merge r6 built the tagged union to prevent. Guarded upstream by `unknown_message`, which is the objection: the safety of this answer becomes a property of a different function.
  - claimed catcher(s): _says it read nothing, rather than reporting a clean scan of nothing_
  - failing tests (2):
    - _never reports a clean scan that read zero messages_
    - _says it read nothing, rather than reporting a clean scan of nothing_
- **`later_revision_blank_statement_reads_clean`** — The same merge on the other two paths: a blank statement, or one with no routing tokens, reported a completed scan of a window it never opened.
  - claimed catcher(s): _says it read nothing, rather than reporting a clean scan of nothing_
  - failing tests (2):
    - _never reports a clean scan that read zero messages_
    - _says it read nothing, rather than reporting a clean scan of nothing_
- **`reducer_type_certification_removed`** — r7's reducer twin. The engine is advice and `appendEvent` is the trust boundary — r5's lesson — so deleting this lets a laundered commitment land as an accepted claim even while the engine refuses to emit one.
  - claimed catcher(s): _refuses the laundered commitment at the reducer, where it is a boundary_; _refuses a model landing a claim whose words could be an undertaking_
  - failing tests (8):
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _model_proposer accepts a claim with a uncertified_type receipt_
    - _never emits an acceptance the reducer would refuse to fold_
    - _reached every gate in the table — walking what the reducer said, not what the oracle predicts_
    - _refuses a commitment-shaped claim at any confidence, above the floor included_
    - _refuses a model landing a claim whose words could be an undertaking_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the laundered commitment at the reducer, where it is a boundary_
- **`commitment_shapes_emptied`** — r7. Empties the marker list, so nothing reads as an undertaking and every claim certifies itself again. The list is admissible only because the proposer cannot choose its input — the statement is the author's own words, proved verbatim by the receipt — and because a hit only ever adds a referral.
  - claimed catcher(s): _refers a claim whose words are equally an undertaking_
  - failing tests (15):
    - _accepts the same reading once the quote is the whole sentence_
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _exercised the adversarial classes r4 added, not just the r3 ones_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _never emits an acceptance the reducer would refuse to fold_
    - _places fixed confidences in the cells the table implies_
    - _reaches every rule name the type declares_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _refers a claim whose words are equally an undertaking_
    - _refuses it at the reducer too, so neither backstops the other by accident_
    - _refuses the laundered commitment at the engine_
    - _refuses the laundered commitment at the reducer, where it is a boundary_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _renders the laundered reading the engine sent to Needs-you_
    - _sees the same words through every difference the receipt forgives_
- **`certification_reads_raw_statement_engine`** — r8's headline. The receipt proves equality under `normalizeForReceipt`; the engine read the raw statement, so one deletable code point evaded every COMMITMENT_SHAPES entry at once.
  - claimed catcher(s): _reads a value the proposer does not control — over the form the receipt proved_; _folds the typographic apostrophe, which needed no adversary at all_
  - failing tests (3):
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
- **`certification_reads_raw_statement_reducer`** — The twin. Both enforcement points called one predicate over one unnormalized text, so neither backstopped the other.
  - claimed catcher(s): _refuses it at the reducer too, so neither backstops the other by accident_
  - failing tests (1):
    - _refuses it at the reducer too, so neither backstops the other by accident_
- **`scan_floor_from_citations`** — r8. The correction scan's floor was `firstCited`, which the proposal controls: with two messages carrying the same body, citing the later copy put the correction behind the scan's start.
  - claimed catcher(s): _finds the correction whichever copy of the sentence is cited_; _reads the same disposition end to end for both citations_
  - failing tests (2):
    - _finds the correction whichever copy of the sentence is cited_
    - _reads the same disposition end to end for both citations_
- **`adjacent_correction_unscanned`** — r8. A correction in the sentence *next to* an exact restatement was missed, using the exact strings `quote_omits_surrounding_text` cites one message earlier.
  - claimed catcher(s): _sees a correction in the sentence next to the restatement_
  - failing tests (1):
    - _sees a correction in the sentence next to the restatement_
- **`sed_marker_reads_raw_body`** — r8's foreign-lineage sweep: `SED_CORRECTION` read the raw body while every retraction marker beside it read the routing fold.
  - claimed catcher(s): _reads the sed marker through the fold its neighbours read through_
  - failing tests (1):
    - _reads the sed marker through the fold its neighbours read through_
- **`cursor_compares_the_spelling`** — r8. `'.'` sorts before `'Z'`, so `…00:00.500Z` compared earlier than `…00:00Z` — half a second later, rejected as out_of_order, unrecoverably.
  - claimed catcher(s): _sorts half a second later after, not before_; _reads every spelling `Timestamp` admits as one order_; _keeps sub-millisecond precision rather than collapsing it onto the id_; _orders a log by when things happened_; _does not reject the later event as out of order, unrecoverably_
  - failing tests (5):
    - _does not reject the later event as out of order, unrecoverably_
    - _keeps sub-millisecond precision rather than collapsing it onto the id_
    - _orders a log by when things happened_
    - _reads every spelling `Timestamp` admits as one order_
    - _sorts half a second later after, not before_
- **`instant_key_truncates_to_milliseconds`** — r8, and the first mutant to touch `common.ts` at all. Folding the fraction into `Date.parse` collides two instants that differ below a millisecond, which orders them by id.
  - claimed catcher(s): _keeps sub-millisecond precision rather than collapsing it onto the id_
  - failing tests (1):
    - _keeps sub-millisecond precision rather than collapsing it onto the id_
- **`retype_skips_structural_invariants`** — r8. No mutant and no test reached past `applyRetype`s payload: it was the one door into states `applyObjectAccepted` and `applyRelationAdded` refuse.
  - claimed catcher(s): _refuses to retype an objective that things are filed under_; _refuses to retype an answered question out of being a question_
  - failing tests (2):
    - _refuses to retype an answered question out of being a question_
    - _refuses to retype an objective that things are filed under_
- **`retype_drops_the_person`** — r8, and the first mutant on `corrections.ts` that is not a display string. Without the attribution carry, a retype round trip moves a commitment's owner with no `reattribute` row.
  - claimed catcher(s): _refuses a retype that moves a commitment onto a different person_; _carries the person across a retype round trip_
  - failing tests (3):
    - _carries the person across a retype round trip_
    - _human performs "retype"_
    - _refuses a retype that moves a commitment onto a different person_
- **`answers_edge_ignores_retraction`** — r8. `answerBindingRefusal` says it is 'checked again by the reducer because a check that only runs upstream is not a check'. The reducer repeated four of seven; this is one it did not.
  - claimed catcher(s): _refuses an edge that would settle a retracted question_
  - failing tests (1):
    - _refuses an edge that would settle a retracted question_
- **`answers_edge_ignores_already_answered`** — r8, and verbatim round 1's gauntlet finding — two `answers` edges on one question — fixed then in the command layer only.
  - claimed catcher(s): _refuses a second answer onto one question — round 1’s finding, at the fold_
  - failing tests (1):
    - _refuses a second answer onto one question — round 1’s finding, at the fold_
- **`supersession_is_irreversible`** — r8. `applyReopen` names 'retract the decision that replaced it' as the remedy for a superseded decision, and on r7 that did nothing at all.
  - claimed catcher(s): _un-retires the replaced decision when the replacement is retracted_
  - failing tests (1):
    - _un-retires the replaced decision when the replacement is retracted_
- **`panel_skips_uncertified_readings`** — r8. `acceptance.ts` promises a laundered reading 'goes to Needs-you with its quote'; `projectAttention` dropped every proposal that was not a decision or a commitment, and returned zero items and zero refusals.
  - claimed catcher(s): _renders the laundered reading the engine sent to Needs-you_; _refuses a laundered reading it has nobody to show, rather than dropping it_
  - failing tests (2):
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _renders the laundered reading the engine sent to Needs-you_
- **`panel_continues_before_the_invariant`** — r9. The defect class in one line: a branch added to the proposal loop that `continue`s before anything counts what it produced. Three rounds shipped one; this is what the invariant test is for.
  - claimed catcher(s): _raises an item or refuses, for every one of them — with a member list_; _raises an item or refuses, for every one of them — without a member list_; _sends a self-stated commitment to its owner whether or not a roster exists_
  - failing tests (7):
    - _asks the named owner to confirm a third-party commitment_
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _cannot be made to collide across the proposal and object namespaces_
    - _follows #6’s order exactly_
    - _raises an item or refuses, for every one of them — with a member list_
    - _raises an item or refuses, for every one of them — without a member list_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
- **`panel_drops_self_stated_commitment`** — r9. `awaitingConfirmFrom` is null for a commitment somebody made about themselves, and the branch used it as the whole audience — so every self-stated commitment the engine sent to Needs-you reached nobody, in a rule whose own reason text says it 'goes to Needs-you for a person to accept or decline'.
  - claimed catcher(s): _sends a self-stated commitment to its owner whether or not a roster exists_; _asks the owner of a self-stated commitment, and not with the third-party sentence_
  - failing tests (2):
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
- **`panel_tells_you_a_colleague_committed_you`** — r9. `commitment_confirm` renders 'somebody else's message named you as the owner of this' — false of a commitment you wrote yourself, and false in the direction that misrepresents the room to the person being asked.
  - claimed catcher(s): _asks the owner of a self-stated commitment, and not with the third-party sentence_; _sends a self-stated commitment to its owner whether or not a roster exists_
  - failing tests (2):
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
- **`panel_silences_an_empty_audience`** — r9. `raiseOrRefuse` is the only thing standing between an empty fanout and silence: with no roster and no `decidedBy`, an unassigned decision produced no item and said nothing about it.
  - claimed catcher(s): _refuses an unassigned decision it has nobody to show, and names the room_; _refuses a laundered reading it has nobody to show, rather than dropping it_; _raises an item or refuses, for every one of them — without a member list_
  - failing tests (4):
    - _names nobody when membership is unknown, and says so instead of guessing_
    - _raises an item or refuses, for every one of them — without a member list_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses an unassigned decision it has nobody to show, and names the room_
- **`reconcile_drops_dismissals`** — r8. A stored non-pending item that stopped being computed was dropped, so a dismissal survived one cycle and not two — and one documented refusal path empties the store.
  - claimed catcher(s): _resolves a pending item whose object no longer generates it_; _keeps a dismissal across a cycle that computes nothing_; _still resolves a pending item that stopped being computed_
  - failing tests (3):
    - _keeps a dismissal across a cycle that computes nothing_
    - _resolves a pending item whose object no longer generates it_
    - _still resolves a pending item that stopped being computed_
- **`fallback_priority_returns_nan`** — r8. `MAX_SAFE_INTEGER + NaN` is `NaN`, a comparator returning `NaN` is not an ordering, and an out-of-enum class sorted above `needs_decision`.
  - claimed catcher(s): _sorts an out-of-enum class last instead of above everything_
  - failing tests (1):
    - _sorts an out-of-enum class last instead of above everything_
- **`mention_items_undeduped`** — r8. Two signals naming one person on one object produced two items with one id, so the projection was not byte-identical across arrival orders.
  - claimed catcher(s): _is byte-identical whichever order the mentions arrive in_
  - failing tests (1):
    - _is byte-identical whichever order the mentions arrive in_
- **`item_id_unescaped`** — r8. Ids interpolated `userId` and the subject id raw, and `Id` has no charset restriction, so one item could inherit another’s dismissal.
  - claimed catcher(s): _cannot be made to collide across the proposal and object namespaces_
  - failing tests (1):
    - _cannot be made to collide across the proposal and object namespaces_
- **`dedup_folds_compatibility_forms`** — r8. NFKC maps `10²` onto `102`, so a reading of 100 was discarded as a duplicate of 102 — silently, on the one path in the engine that destroys a reading.
  - claimed catcher(s): _keeps `10² ms` and `102 ms` apart_
  - failing tests (12):
    - _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
    - _discards a re-proposal of something already accepted_
    - _discards only a re-proposal of the same sentence_
    - _does not discard a quantifier substitution either_
    - _does not discard a reading that says the opposite of the accepted one_
    - _does not discard the negation of an accepted claim as a re-proposal of it_
    - _does not match across types_
    - _keeps `10² ms` and `102 ms` apart_
    - _r9 — keeps a re-cased literal apart from the literal it is not_
    - _reaches every rule name the type declares_
    - _requires both statement similarity and provenance overlap_
    - _still fires on the same sentence proposed twice_
- **`dedup_folds_case`** — r9. r8's surviving fold. The lower-casing ran through the code segment `normalizeForReceipt` preserves byte for byte, so a correction from `Hunter2` to `hunter2` was discarded and the record kept the old password — no proposal, no issue, no trace.
  - claimed catcher(s): _r9 — keeps a re-cased literal apart from the literal it is not_; _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
  - failing tests (2):
    - _a machine-accepted reading cannot be destroyed by a re-cased duplicate_
    - _r9 — keeps a re-cased literal apart from the literal it is not_
- **`question_ornaments_unknown`** — r8. `❓`/`❔` were in neither the source list nor the test list that pinned it, so a question minted as a claim auto-accepted — r5's defect, reopened.
  - claimed catcher(s): _refuses an ornament question mark minted as a claim — r5’s defect, reopened_
  - failing tests (1):
    - _refuses an ornament question mark minted as a claim — r5’s defect, reopened_
- **`question_certification_is_the_negation`** — r8. The two speech-act checks are mirrors with opposite safe directions; reading one generous set for both auto-accepts a declarative at `open_question`s lower θ.
  - claimed catcher(s): _refuses an assertion on an unverifiable mark without certifying a question_
  - failing tests (1):
    - _refuses an assertion on an unverifiable mark without certifying a question_
- **`payload_text_reads_the_wrong_key`** — r8, and the first mutant on `objects.ts`. `payloadText` is what every text predicate in the package is handed; reading the wrong key for one type makes all of them blind to it.
  - claimed catcher(s): _places fixed confidences in the cells the table implies_
  - failing tests (7):
    - _drives every needs-you-capable rule through a real staged proposal_
    - _objective at θ_auto_
    - _objective below θ_min_
    - _objective in the θ band_
    - _places fixed confidences in the cells the table implies_
    - _raises an item or refuses, for every one of them — with a member list_
    - _raises an item or refuses, for every one of them — without a member list_
- **`correction_rate_counts_every_correction`** — r8. `correctionRateByType` is #5/#17s live quality metric and had no mutant; counting corrections rather than corrected objects inflates the rate silently.
  - claimed catcher(s): _counts objects corrected, not corrections_
  - failing tests (1):
    - _counts objects corrected, not corrections_
- **`watermark_never_advances`** — r8. The per-room watermark is what #22's `core_events.room_seq` maps onto and no mutant touched it.
  - claimed catcher(s): _tracks the last consumed position per room_; _tracks each room separately while the gate stays global_; _consumes — and advances — an event refused on business grounds_
  - failing tests (3):
    - _consumes — and advances — an event refused on business grounds_
    - _tracks each room separately while the gate stays global_
    - _tracks the last consumed position per room_
- **`canonical_json_keeps_key_order`** — r8, and the first mutant on `state.ts`. `serializeState` is the instrument every determinism and live≡replay test measures with; unsorted keys make all of them assert nothing about order.
  - claimed catcher(s): _serializes a state with its keys sorted, whatever order they were built in_
  - failing tests (1):
    - _serializes a state with its keys sorted, whatever order they were built in_
- **`engine_always_auto_accepts`** — r8's test-scoping finding. Gutting `decideAcceptance` to an unconditional `auto_accept` left `guards.test.ts` and `replay.test.ts` entirely green — the wider suite caught it, so this was never a hole, but two files named for the trust boundary could not see the engine half of it. This row exists so the scoping is measured rather than assumed.
  - claimed catcher(s): _never emits an acceptance the reducer would refuse to fold_; _discards a reading the table says to discard rather than surfacing it_
  - failing tests (91):
    - _accepts the same reading once the quote is the whole sentence_
    - _accepts the same shape once the quote carries the sentence_
    - _accepts the sentence Bob actually wrote, so the rule is not "refuse everything"_
    - _allows a stricter override_
    - _asks a human-staged commitment’s named owner to confirm, whoever staged it_
    - _asks the named owner to confirm a third-party commitment_
    - _asks the owner of a self-stated commitment, and not with the third-party sentence_
    - _cannot be helped by dropping the citation that carries the quote_
    - _cannot be made to collide across the proposal and object namespaces_
    - _claim below θ_min_
    - _claim in the θ band_
    - _commitment (self) at θ_auto_
    - _commitment (self) below θ_min_
    - _commitment (self) in the θ band_
    - _commitment (third-party) at θ_auto_
    - _commitment (third_party) below θ_min_
    - _commitment (third_party) in the θ band_
    - _decision at confidence 1.0_
    - _decision at θ_auto_
    - _decision below θ_min_
    - _decision in the θ band_
    - _discards a 0.98-confidence claim whose receipt is wrong_
    - _discards a hair under θ_min (0.4999)_
    - _discards a model proposal judged with no messages, at any confidence_
    - _discards a re-proposal of something already accepted_
    - _discards a reading the table says to discard rather than surfacing it_
    - _discards it for every type, so no type has a quiet way through_
    - _does not accept a hair under θ_auto (0.6999)_
    - _does not auto-accept it at the engine_
    - _does not auto-accept it at the engine either_
    - _does not count a sentence the cited author only quoted back_
    - _does not let deduplication destroy a discrepancy nobody has seen_
    - _drives every needs-you-capable rule through a real staged proposal_
    - _enforces minQuoteLength on the acceptance path, not only on blockquotes_
    - _fans an unassigned decision out to every member — "or any-member when unassigned"_
    - _folds the typographic apostrophe, which needed no adversary at all_
    - _follows #6’s order exactly_
    - _keeps a link title, which is text the author wrote_
    - _keeps a proposal exactly at θ_min (0.5) rather than discarding it_
    - _keeps the disposition — an unread body is not a clean one_
    - _keeps the verdict, which was never the defect_
    - _leaves the reading staged rather than deleting it, so a person sees the discrepancy_
    - _leaves the θ table alone below θ_auto, which the first implementation did not_
    - _names nobody when membership is unknown, and says so instead of guessing_
    - _never emits an acceptance the reducer would refuse to fold_
    - _objective at θ_auto_
    - _objective below θ_min_
    - _objective in the θ band_
    - _open_question below θ_min_
    - _open_question in the θ band_
    - _places fixed confidences in the cells the table implies_
    - _raises an item or refuses, for every one of them — with a member list_
    - _raises an item or refuses, for every one of them — without a member list_
    - _reaches every rule name the type declares_
    - _reads a value the proposer does not control — over the form the receipt proved_
    - _reads the plain ways of undertaking something, not only the hedged ones_
    - _refers a claim whose words are equally an undertaking_
    - _refers a paraphrase of the quote to a person instead of accepting it_
    - _refers an inverted receipt that sits below theta_min_
    - _refers it rather than rejecting it_
    - _refers when the second telling adds words to the first_
    - _refers, rather than destroys, the model that drops the trailing full stop_
    - _refuses `messages: []` at the attention panel, with a receipt for the silence_
    - _refuses `messages: []` at the engine, exactly as it refuses no messages_
    - _refuses a laundered reading it has nobody to show, rather than dropping it_
    - _refuses a model-accepted objective too_
    - _refuses a model-accepted third-party commitment at the floor_
    - _refuses a quote mined out of a first-person refusal_
    - _refuses a quote mined out of a hedge_
    - _refuses a quote mined out of a report of what nobody thinks_
    - _refuses a quote mined out of an explicit denial_
    - _refuses a verbatim, correctly attributed quote that carries a different sentence_
    - _refuses a window of messages that exist and say nothing, at the engine_
    - _refuses a window that stops where the citations stop_
    - _refuses an unassigned decision it has nobody to show, and names the room_
    - _refuses as ambiguous when two cited authors both carry the quote_
    - _refuses every one of the four at the engine_
    - _refuses the code literal a dropped full stop used to rewrite_
    - _refuses the exact pair from the gauntlet at the acceptance engine_
    - _refuses the laundered commitment at the engine_
    - _refuses the padded multi-citation window at both enforcement points_
    - _refuses the spliced statement at the engine, as it refuses the verbatim one_
    - _refuses to mint a code literal the author did not write, at every layer_
    - _refuses to raise a confirm it cannot justify, rather than asking everybody_
    - _renders the laundered reading the engine sent to Needs-you_
    - _routes a named decision to the person named_
    - _sends a self-stated commitment to its owner whether or not a roster exists_
    - _silences the confirm request and records why, rather than asking Bob to confirm it_
    - _still discards a weak reading whose receipt is clean_
    - _still judges a human-staged proposal, which needs no receipt_
    - _stops firing on four sentences that undertake nothing_
